"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: boss/v2/upload_file_ctr.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.UploadFileCTRResponse = exports.UploadFileCTRRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const ctr_boss_flags_1 = require("./ctr_boss_flags");
const file_attributes_1 = require("./file_attributes");
const file_ctr_1 = require("./file_ctr");
const payload_content_ctr_1 = require("./payload_content_ctr");
exports.protobufPackage = "boss.v2";
function createBaseUploadFileCTRRequest() {
    return {
        taskId: "",
        bossAppId: "",
        supportedCountries: [],
        supportedLanguages: [],
        attributes: undefined,
        name: "",
        serialNumber: 0n,
        payloadContents: [],
        flags: undefined,
    };
}
exports.UploadFileCTRRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.taskId !== "") {
            writer.uint32(10).string(message.taskId);
        }
        if (message.bossAppId !== "") {
            writer.uint32(18).string(message.bossAppId);
        }
        for (const v of message.supportedCountries) {
            writer.uint32(26).string(v);
        }
        for (const v of message.supportedLanguages) {
            writer.uint32(34).string(v);
        }
        if (message.attributes !== undefined) {
            file_attributes_1.FileAttributes.encode(message.attributes, writer.uint32(42).fork()).join();
        }
        if (message.name !== "") {
            writer.uint32(50).string(message.name);
        }
        if (message.serialNumber !== 0n) {
            if (BigInt.asUintN(64, message.serialNumber) !== message.serialNumber) {
                throw new globalThis.Error("value provided for field message.serialNumber of type uint64 too large");
            }
            writer.uint32(56).uint64(message.serialNumber);
        }
        for (const v of message.payloadContents) {
            payload_content_ctr_1.PayloadContentCTR.encode(v, writer.uint32(66).fork()).join();
        }
        if (message.flags !== undefined) {
            ctr_boss_flags_1.CTRBOSSFlags.encode(message.flags, writer.uint32(74).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadFileCTRRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.taskId = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.bossAppId = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.supportedCountries.push(reader.string());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.supportedLanguages.push(reader.string());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.attributes = file_attributes_1.FileAttributes.decode(reader, reader.uint32());
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.serialNumber = reader.uint64();
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.payloadContents.push(payload_content_ctr_1.PayloadContentCTR.decode(reader, reader.uint32()));
                    continue;
                }
                case 9: {
                    if (tag !== 74) {
                        break;
                    }
                    message.flags = ctr_boss_flags_1.CTRBOSSFlags.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
            bossAppId: isSet(object.bossAppId) ? globalThis.String(object.bossAppId) : "",
            supportedCountries: globalThis.Array.isArray(object?.supportedCountries)
                ? object.supportedCountries.map((e) => globalThis.String(e))
                : [],
            supportedLanguages: globalThis.Array.isArray(object?.supportedLanguages)
                ? object.supportedLanguages.map((e) => globalThis.String(e))
                : [],
            attributes: isSet(object.attributes) ? file_attributes_1.FileAttributes.fromJSON(object.attributes) : undefined,
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            serialNumber: isSet(object.serialNumber) ? BigInt(object.serialNumber) : 0n,
            payloadContents: globalThis.Array.isArray(object?.payloadContents)
                ? object.payloadContents.map((e) => payload_content_ctr_1.PayloadContentCTR.fromJSON(e))
                : [],
            flags: isSet(object.flags) ? ctr_boss_flags_1.CTRBOSSFlags.fromJSON(object.flags) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.taskId !== "") {
            obj.taskId = message.taskId;
        }
        if (message.bossAppId !== "") {
            obj.bossAppId = message.bossAppId;
        }
        if (message.supportedCountries?.length) {
            obj.supportedCountries = message.supportedCountries;
        }
        if (message.supportedLanguages?.length) {
            obj.supportedLanguages = message.supportedLanguages;
        }
        if (message.attributes !== undefined) {
            obj.attributes = file_attributes_1.FileAttributes.toJSON(message.attributes);
        }
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.serialNumber !== 0n) {
            obj.serialNumber = message.serialNumber.toString();
        }
        if (message.payloadContents?.length) {
            obj.payloadContents = message.payloadContents.map((e) => payload_content_ctr_1.PayloadContentCTR.toJSON(e));
        }
        if (message.flags !== undefined) {
            obj.flags = ctr_boss_flags_1.CTRBOSSFlags.toJSON(message.flags);
        }
        return obj;
    },
    create(base) {
        return exports.UploadFileCTRRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadFileCTRRequest();
        message.taskId = object.taskId ?? "";
        message.bossAppId = object.bossAppId ?? "";
        message.supportedCountries = object.supportedCountries?.map((e) => e) || [];
        message.supportedLanguages = object.supportedLanguages?.map((e) => e) || [];
        message.attributes = (object.attributes !== undefined && object.attributes !== null)
            ? file_attributes_1.FileAttributes.fromPartial(object.attributes)
            : undefined;
        message.name = object.name ?? "";
        message.serialNumber = object.serialNumber ?? 0n;
        message.payloadContents = object.payloadContents?.map((e) => payload_content_ctr_1.PayloadContentCTR.fromPartial(e)) || [];
        message.flags = (object.flags !== undefined && object.flags !== null)
            ? ctr_boss_flags_1.CTRBOSSFlags.fromPartial(object.flags)
            : undefined;
        return message;
    },
};
function createBaseUploadFileCTRResponse() {
    return { file: undefined };
}
exports.UploadFileCTRResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.file !== undefined) {
            file_ctr_1.FileCTR.encode(message.file, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUploadFileCTRResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.file = file_ctr_1.FileCTR.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { file: isSet(object.file) ? file_ctr_1.FileCTR.fromJSON(object.file) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.file !== undefined) {
            obj.file = file_ctr_1.FileCTR.toJSON(message.file);
        }
        return obj;
    },
    create(base) {
        return exports.UploadFileCTRResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUploadFileCTRResponse();
        message.file = (object.file !== undefined && object.file !== null) ? file_ctr_1.FileCTR.fromPartial(object.file) : undefined;
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
