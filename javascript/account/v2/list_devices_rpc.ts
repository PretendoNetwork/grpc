// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/list_devices_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Device } from "./device";
import { PageInput, PageResults } from "./page";

export const protobufPackage = "account.v2";

export interface ListDevicesRequest {
  page: PageInput | undefined;
  filter: ListDevicesRequest_Filter | undefined;
}

export interface ListDevicesRequest_Filter {
  pid: number[];
  deviceId?: number | undefined;
  serial?: string | undefined;
  extraJson?: string | undefined;
}

export interface ListDevicesResponse {
  page: PageResults | undefined;
  items: Device[];
}

function createBaseListDevicesRequest(): ListDevicesRequest {
  return { page: undefined, filter: undefined };
}

export const ListDevicesRequest: MessageFns<ListDevicesRequest> = {
  encode(message: ListDevicesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageInput.encode(message.page, writer.uint32(10).fork()).join();
    }
    if (message.filter !== undefined) {
      ListDevicesRequest_Filter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDevicesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDevicesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageInput.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = ListDevicesRequest_Filter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDevicesRequest {
    return {
      page: isSet(object.page) ? PageInput.fromJSON(object.page) : undefined,
      filter: isSet(object.filter) ? ListDevicesRequest_Filter.fromJSON(object.filter) : undefined,
    };
  },

  toJSON(message: ListDevicesRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageInput.toJSON(message.page);
    }
    if (message.filter !== undefined) {
      obj.filter = ListDevicesRequest_Filter.toJSON(message.filter);
    }
    return obj;
  },

  create(base?: DeepPartial<ListDevicesRequest>): ListDevicesRequest {
    return ListDevicesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListDevicesRequest>): ListDevicesRequest {
    const message = createBaseListDevicesRequest();
    message.page = (object.page !== undefined && object.page !== null) ? PageInput.fromPartial(object.page) : undefined;
    message.filter = (object.filter !== undefined && object.filter !== null)
      ? ListDevicesRequest_Filter.fromPartial(object.filter)
      : undefined;
    return message;
  },
};

function createBaseListDevicesRequest_Filter(): ListDevicesRequest_Filter {
  return { pid: [], deviceId: undefined, serial: undefined, extraJson: undefined };
}

export const ListDevicesRequest_Filter: MessageFns<ListDevicesRequest_Filter> = {
  encode(message: ListDevicesRequest_Filter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.pid) {
      writer.uint32(v);
    }
    writer.join();
    if (message.deviceId !== undefined) {
      writer.uint32(16).uint32(message.deviceId);
    }
    if (message.serial !== undefined) {
      writer.uint32(26).string(message.serial);
    }
    if (message.extraJson !== undefined) {
      writer.uint32(34).string(message.extraJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDevicesRequest_Filter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDevicesRequest_Filter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.pid.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.pid.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.deviceId = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.serial = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.extraJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDevicesRequest_Filter {
    return {
      pid: globalThis.Array.isArray(object?.pid) ? object.pid.map((e: any) => globalThis.Number(e)) : [],
      deviceId: isSet(object.deviceId) ? globalThis.Number(object.deviceId) : undefined,
      serial: isSet(object.serial) ? globalThis.String(object.serial) : undefined,
      extraJson: isSet(object.extraJson) ? globalThis.String(object.extraJson) : undefined,
    };
  },

  toJSON(message: ListDevicesRequest_Filter): unknown {
    const obj: any = {};
    if (message.pid?.length) {
      obj.pid = message.pid.map((e) => Math.round(e));
    }
    if (message.deviceId !== undefined) {
      obj.deviceId = Math.round(message.deviceId);
    }
    if (message.serial !== undefined) {
      obj.serial = message.serial;
    }
    if (message.extraJson !== undefined) {
      obj.extraJson = message.extraJson;
    }
    return obj;
  },

  create(base?: DeepPartial<ListDevicesRequest_Filter>): ListDevicesRequest_Filter {
    return ListDevicesRequest_Filter.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListDevicesRequest_Filter>): ListDevicesRequest_Filter {
    const message = createBaseListDevicesRequest_Filter();
    message.pid = object.pid?.map((e) => e) || [];
    message.deviceId = object.deviceId ?? undefined;
    message.serial = object.serial ?? undefined;
    message.extraJson = object.extraJson ?? undefined;
    return message;
  },
};

function createBaseListDevicesResponse(): ListDevicesResponse {
  return { page: undefined, items: [] };
}

export const ListDevicesResponse: MessageFns<ListDevicesResponse> = {
  encode(message: ListDevicesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageResults.encode(message.page, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      Device.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDevicesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDevicesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageResults.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(Device.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDevicesResponse {
    return {
      page: isSet(object.page) ? PageResults.fromJSON(object.page) : undefined,
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => Device.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListDevicesResponse): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageResults.toJSON(message.page);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => Device.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListDevicesResponse>): ListDevicesResponse {
    return ListDevicesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListDevicesResponse>): ListDevicesResponse {
    const message = createBaseListDevicesResponse();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResults.fromPartial(object.page)
      : undefined;
    message.items = object.items?.map((e) => Device.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
