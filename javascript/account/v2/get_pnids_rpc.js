"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/get_pnids_rpc.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.GetPNIDsResponse_UserDataEntry = exports.GetPNIDsResponse = exports.GetPNIDsRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const get_pnid_rpc_1 = require("./get_pnid_rpc");
exports.protobufPackage = "account.v2";
function createBaseGetPNIDsRequest() {
    return { pid: [] };
}
exports.GetPNIDsRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        writer.uint32(10).fork();
        for (const v of message.pid) {
            writer.uint32(v);
        }
        writer.join();
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetPNIDsRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag === 8) {
                        message.pid.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.pid.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { pid: globalThis.Array.isArray(object?.pid) ? object.pid.map((e) => globalThis.Number(e)) : [] };
    },
    toJSON(message) {
        const obj = {};
        if (message.pid?.length) {
            obj.pid = message.pid.map((e) => Math.round(e));
        }
        return obj;
    },
    create(base) {
        return exports.GetPNIDsRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetPNIDsRequest();
        message.pid = object.pid?.map((e) => e) || [];
        return message;
    },
};
function createBaseGetPNIDsResponse() {
    return { userData: {} };
}
exports.GetPNIDsResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        Object.entries(message.userData).forEach(([key, value]) => {
            exports.GetPNIDsResponse_UserDataEntry.encode({ key: key, value }, writer.uint32(10).fork()).join();
        });
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetPNIDsResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    const entry1 = exports.GetPNIDsResponse_UserDataEntry.decode(reader, reader.uint32());
                    if (entry1.value !== undefined) {
                        message.userData[entry1.key] = entry1.value;
                    }
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            userData: isObject(object.userData)
                ? Object.entries(object.userData).reduce((acc, [key, value]) => {
                    acc[globalThis.Number(key)] = get_pnid_rpc_1.GetPNIDResponse.fromJSON(value);
                    return acc;
                }, {})
                : {},
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.userData) {
            const entries = Object.entries(message.userData);
            if (entries.length > 0) {
                obj.userData = {};
                entries.forEach(([k, v]) => {
                    obj.userData[k] = get_pnid_rpc_1.GetPNIDResponse.toJSON(v);
                });
            }
        }
        return obj;
    },
    create(base) {
        return exports.GetPNIDsResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetPNIDsResponse();
        message.userData = Object.entries(object.userData ?? {}).reduce((acc, [key, value]) => {
            if (value !== undefined) {
                acc[globalThis.Number(key)] = get_pnid_rpc_1.GetPNIDResponse.fromPartial(value);
            }
            return acc;
        }, {});
        return message;
    },
};
function createBaseGetPNIDsResponse_UserDataEntry() {
    return { key: 0, value: undefined };
}
exports.GetPNIDsResponse_UserDataEntry = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.key !== 0) {
            writer.uint32(8).uint32(message.key);
        }
        if (message.value !== undefined) {
            get_pnid_rpc_1.GetPNIDResponse.encode(message.value, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetPNIDsResponse_UserDataEntry();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.key = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.value = get_pnid_rpc_1.GetPNIDResponse.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            key: isSet(object.key) ? globalThis.Number(object.key) : 0,
            value: isSet(object.value) ? get_pnid_rpc_1.GetPNIDResponse.fromJSON(object.value) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.key !== 0) {
            obj.key = Math.round(message.key);
        }
        if (message.value !== undefined) {
            obj.value = get_pnid_rpc_1.GetPNIDResponse.toJSON(message.value);
        }
        return obj;
    },
    create(base) {
        return exports.GetPNIDsResponse_UserDataEntry.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetPNIDsResponse_UserDataEntry();
        message.key = object.key ?? 0;
        message.value = (object.value !== undefined && object.value !== null)
            ? get_pnid_rpc_1.GetPNIDResponse.fromPartial(object.value)
            : undefined;
        return message;
    },
};
function isObject(value) {
    return typeof value === "object" && value !== null;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
