// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/get_nex_account_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { NEXAccount } from "./nex_account";

export const protobufPackage = "account.v2";

export interface GetNEXAccountRequest {
  pid: number;
}

export interface GetNEXAccountResponse {
  nexAccount: NEXAccount | undefined;
}

function createBaseGetNEXAccountRequest(): GetNEXAccountRequest {
  return { pid: 0 };
}

export const GetNEXAccountRequest: MessageFns<GetNEXAccountRequest> = {
  encode(message: GetNEXAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetNEXAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetNEXAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetNEXAccountRequest {
    return { pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0 };
  },

  toJSON(message: GetNEXAccountRequest): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    return obj;
  },

  create(base?: DeepPartial<GetNEXAccountRequest>): GetNEXAccountRequest {
    return GetNEXAccountRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetNEXAccountRequest>): GetNEXAccountRequest {
    const message = createBaseGetNEXAccountRequest();
    message.pid = object.pid ?? 0;
    return message;
  },
};

function createBaseGetNEXAccountResponse(): GetNEXAccountResponse {
  return { nexAccount: undefined };
}

export const GetNEXAccountResponse: MessageFns<GetNEXAccountResponse> = {
  encode(message: GetNEXAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nexAccount !== undefined) {
      NEXAccount.encode(message.nexAccount, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetNEXAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetNEXAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nexAccount = NEXAccount.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetNEXAccountResponse {
    return { nexAccount: isSet(object.nexAccount) ? NEXAccount.fromJSON(object.nexAccount) : undefined };
  },

  toJSON(message: GetNEXAccountResponse): unknown {
    const obj: any = {};
    if (message.nexAccount !== undefined) {
      obj.nexAccount = NEXAccount.toJSON(message.nexAccount);
    }
    return obj;
  },

  create(base?: DeepPartial<GetNEXAccountResponse>): GetNEXAccountResponse {
    return GetNEXAccountResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetNEXAccountResponse>): GetNEXAccountResponse {
    const message = createBaseGetNEXAccountResponse();
    message.nexAccount = (object.nexAccount !== undefined && object.nexAccount !== null)
      ? NEXAccount.fromPartial(object.nexAccount)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
