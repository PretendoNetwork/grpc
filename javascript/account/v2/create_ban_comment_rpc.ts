// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/create_ban_comment_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Comment } from "./comment";

export const protobufPackage = "account.v2";

export interface CreateBanCommentRequest {
  id: string;
  content: string;
}

export interface CreateBanCommentResponse {
  item: Comment | undefined;
}

function createBaseCreateBanCommentRequest(): CreateBanCommentRequest {
  return { id: "", content: "" };
}

export const CreateBanCommentRequest: MessageFns<CreateBanCommentRequest> = {
  encode(message: CreateBanCommentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateBanCommentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateBanCommentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateBanCommentRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: CreateBanCommentRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateBanCommentRequest>): CreateBanCommentRequest {
    return CreateBanCommentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateBanCommentRequest>): CreateBanCommentRequest {
    const message = createBaseCreateBanCommentRequest();
    message.id = object.id ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseCreateBanCommentResponse(): CreateBanCommentResponse {
  return { item: undefined };
}

export const CreateBanCommentResponse: MessageFns<CreateBanCommentResponse> = {
  encode(message: CreateBanCommentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.item !== undefined) {
      Comment.encode(message.item, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateBanCommentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateBanCommentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.item = Comment.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateBanCommentResponse {
    return { item: isSet(object.item) ? Comment.fromJSON(object.item) : undefined };
  },

  toJSON(message: CreateBanCommentResponse): unknown {
    const obj: any = {};
    if (message.item !== undefined) {
      obj.item = Comment.toJSON(message.item);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateBanCommentResponse>): CreateBanCommentResponse {
    return CreateBanCommentResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateBanCommentResponse>): CreateBanCommentResponse {
    const message = createBaseCreateBanCommentResponse();
    message.item = (object.item !== undefined && object.item !== null) ? Comment.fromPartial(object.item) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
