// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/update_ban_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { Ban, BanScopeType, banScopeTypeFromJSON, banScopeTypeToJSON } from "./ban_details";

export const protobufPackage = "account.v2";

export interface UpdateBanRequest {
  id: string;
  startDate?: Date | undefined;
  endDate?: Date | undefined;
  scopeType?: BanScopeType | undefined;
  scopeTarget?: string | undefined;
  reason?: string | undefined;
  pardonReason?: string | undefined;
}

export interface UpdateBanResponse {
  ban: Ban | undefined;
}

function createBaseUpdateBanRequest(): UpdateBanRequest {
  return {
    id: "",
    startDate: undefined,
    endDate: undefined,
    scopeType: undefined,
    scopeTarget: undefined,
    reason: undefined,
    pardonReason: undefined,
  };
}

export const UpdateBanRequest: MessageFns<UpdateBanRequest> = {
  encode(message: UpdateBanRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.startDate !== undefined) {
      Timestamp.encode(toTimestamp(message.startDate), writer.uint32(34).fork()).join();
    }
    if (message.endDate !== undefined) {
      Timestamp.encode(toTimestamp(message.endDate), writer.uint32(42).fork()).join();
    }
    if (message.scopeType !== undefined) {
      writer.uint32(48).int32(message.scopeType);
    }
    if (message.scopeTarget !== undefined) {
      writer.uint32(58).string(message.scopeTarget);
    }
    if (message.reason !== undefined) {
      writer.uint32(66).string(message.reason);
    }
    if (message.pardonReason !== undefined) {
      writer.uint32(74).string(message.pardonReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateBanRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateBanRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.scopeType = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.scopeTarget = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.pardonReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateBanRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      startDate: isSet(object.startDate) ? fromJsonTimestamp(object.startDate) : undefined,
      endDate: isSet(object.endDate) ? fromJsonTimestamp(object.endDate) : undefined,
      scopeType: isSet(object.scopeType) ? banScopeTypeFromJSON(object.scopeType) : undefined,
      scopeTarget: isSet(object.scopeTarget) ? globalThis.String(object.scopeTarget) : undefined,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
      pardonReason: isSet(object.pardonReason) ? globalThis.String(object.pardonReason) : undefined,
    };
  },

  toJSON(message: UpdateBanRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.startDate !== undefined) {
      obj.startDate = message.startDate.toISOString();
    }
    if (message.endDate !== undefined) {
      obj.endDate = message.endDate.toISOString();
    }
    if (message.scopeType !== undefined) {
      obj.scopeType = banScopeTypeToJSON(message.scopeType);
    }
    if (message.scopeTarget !== undefined) {
      obj.scopeTarget = message.scopeTarget;
    }
    if (message.reason !== undefined) {
      obj.reason = message.reason;
    }
    if (message.pardonReason !== undefined) {
      obj.pardonReason = message.pardonReason;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateBanRequest>): UpdateBanRequest {
    return UpdateBanRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateBanRequest>): UpdateBanRequest {
    const message = createBaseUpdateBanRequest();
    message.id = object.id ?? "";
    message.startDate = object.startDate ?? undefined;
    message.endDate = object.endDate ?? undefined;
    message.scopeType = object.scopeType ?? undefined;
    message.scopeTarget = object.scopeTarget ?? undefined;
    message.reason = object.reason ?? undefined;
    message.pardonReason = object.pardonReason ?? undefined;
    return message;
  },
};

function createBaseUpdateBanResponse(): UpdateBanResponse {
  return { ban: undefined };
}

export const UpdateBanResponse: MessageFns<UpdateBanResponse> = {
  encode(message: UpdateBanResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ban !== undefined) {
      Ban.encode(message.ban, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateBanResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateBanResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ban = Ban.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateBanResponse {
    return { ban: isSet(object.ban) ? Ban.fromJSON(object.ban) : undefined };
  },

  toJSON(message: UpdateBanResponse): unknown {
    const obj: any = {};
    if (message.ban !== undefined) {
      obj.ban = Ban.toJSON(message.ban);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateBanResponse>): UpdateBanResponse {
    return UpdateBanResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateBanResponse>): UpdateBanResponse {
    const message = createBaseUpdateBanResponse();
    message.ban = (object.ban !== undefined && object.ban !== null) ? Ban.fromPartial(object.ban) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
