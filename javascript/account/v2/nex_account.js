"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/nex_account.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.NEXAccount = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
exports.protobufPackage = "account.v2";
function createBaseNEXAccount() {
    return {
        pid: 0,
        owningPid: undefined,
        accessLevel: 0,
        serverAccessLevel: "",
        friendCode: undefined,
        deviceType: undefined,
    };
}
exports.NEXAccount = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.pid !== 0) {
            writer.uint32(8).uint32(message.pid);
        }
        if (message.owningPid !== undefined) {
            writer.uint32(16).uint32(message.owningPid);
        }
        if (message.accessLevel !== 0) {
            writer.uint32(24).int32(message.accessLevel);
        }
        if (message.serverAccessLevel !== "") {
            writer.uint32(34).string(message.serverAccessLevel);
        }
        if (message.friendCode !== undefined) {
            writer.uint32(42).string(message.friendCode);
        }
        if (message.deviceType !== undefined) {
            writer.uint32(50).string(message.deviceType);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseNEXAccount();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.pid = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.owningPid = reader.uint32();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.accessLevel = reader.int32();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.serverAccessLevel = reader.string();
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.friendCode = reader.string();
                    continue;
                }
                case 6: {
                    if (tag !== 50) {
                        break;
                    }
                    message.deviceType = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
            owningPid: isSet(object.owningPid) ? globalThis.Number(object.owningPid) : undefined,
            accessLevel: isSet(object.accessLevel) ? globalThis.Number(object.accessLevel) : 0,
            serverAccessLevel: isSet(object.serverAccessLevel) ? globalThis.String(object.serverAccessLevel) : "",
            friendCode: isSet(object.friendCode) ? globalThis.String(object.friendCode) : undefined,
            deviceType: isSet(object.deviceType) ? globalThis.String(object.deviceType) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.pid !== 0) {
            obj.pid = Math.round(message.pid);
        }
        if (message.owningPid !== undefined) {
            obj.owningPid = Math.round(message.owningPid);
        }
        if (message.accessLevel !== 0) {
            obj.accessLevel = Math.round(message.accessLevel);
        }
        if (message.serverAccessLevel !== "") {
            obj.serverAccessLevel = message.serverAccessLevel;
        }
        if (message.friendCode !== undefined) {
            obj.friendCode = message.friendCode;
        }
        if (message.deviceType !== undefined) {
            obj.deviceType = message.deviceType;
        }
        return obj;
    },
    create(base) {
        return exports.NEXAccount.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseNEXAccount();
        message.pid = object.pid ?? 0;
        message.owningPid = object.owningPid ?? undefined;
        message.accessLevel = object.accessLevel ?? 0;
        message.serverAccessLevel = object.serverAccessLevel ?? "";
        message.friendCode = object.friendCode ?? undefined;
        message.deviceType = object.deviceType ?? undefined;
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
