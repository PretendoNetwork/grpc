"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/list_bans_rpc.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.ListBansResponse = exports.ListBansRequest_Filter = exports.ListBansRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const timestamp_1 = require("../../google/protobuf/timestamp");
const ban_details_1 = require("./ban_details");
const page_1 = require("./page");
exports.protobufPackage = "account.v2";
function createBaseListBansRequest() {
    return { page: undefined, filter: undefined };
}
exports.ListBansRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.page !== undefined) {
            page_1.PageInput.encode(message.page, writer.uint32(10).fork()).join();
        }
        if (message.filter !== undefined) {
            exports.ListBansRequest_Filter.encode(message.filter, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListBansRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.page = page_1.PageInput.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.filter = exports.ListBansRequest_Filter.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            page: isSet(object.page) ? page_1.PageInput.fromJSON(object.page) : undefined,
            filter: isSet(object.filter) ? exports.ListBansRequest_Filter.fromJSON(object.filter) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.page !== undefined) {
            obj.page = page_1.PageInput.toJSON(message.page);
        }
        if (message.filter !== undefined) {
            obj.filter = exports.ListBansRequest_Filter.toJSON(message.filter);
        }
        return obj;
    },
    create(base) {
        return exports.ListBansRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListBansRequest();
        message.page = (object.page !== undefined && object.page !== null) ? page_1.PageInput.fromPartial(object.page) : undefined;
        message.filter = (object.filter !== undefined && object.filter !== null)
            ? exports.ListBansRequest_Filter.fromPartial(object.filter)
            : undefined;
        return message;
    },
};
function createBaseListBansRequest_Filter() {
    return {
        targetType: [],
        target: [],
        issuer: [],
        startDate: undefined,
        endDate: undefined,
        scopeType: [],
        scopeTarget: [],
        issuedDate: undefined,
    };
}
exports.ListBansRequest_Filter = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        writer.uint32(10).fork();
        for (const v of message.targetType) {
            writer.int32(v);
        }
        writer.join();
        for (const v of message.target) {
            writer.uint32(18).string(v);
        }
        for (const v of message.issuer) {
            writer.uint32(26).string(v);
        }
        if (message.startDate !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.startDate), writer.uint32(34).fork()).join();
        }
        if (message.endDate !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.endDate), writer.uint32(42).fork()).join();
        }
        writer.uint32(50).fork();
        for (const v of message.scopeType) {
            writer.int32(v);
        }
        writer.join();
        for (const v of message.scopeTarget) {
            writer.uint32(58).string(v);
        }
        if (message.issuedDate !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.issuedDate), writer.uint32(66).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListBansRequest_Filter();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag === 8) {
                        message.targetType.push(reader.int32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.targetType.push(reader.int32());
                        }
                        continue;
                    }
                    break;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.target.push(reader.string());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.issuer.push(reader.string());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.startDate = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.endDate = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 6: {
                    if (tag === 48) {
                        message.scopeType.push(reader.int32());
                        continue;
                    }
                    if (tag === 50) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.scopeType.push(reader.int32());
                        }
                        continue;
                    }
                    break;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.scopeTarget.push(reader.string());
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.issuedDate = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            targetType: globalThis.Array.isArray(object?.targetType)
                ? object.targetType.map((e) => (0, ban_details_1.banTargetTypeFromJSON)(e))
                : [],
            target: globalThis.Array.isArray(object?.target) ? object.target.map((e) => globalThis.String(e)) : [],
            issuer: globalThis.Array.isArray(object?.issuer) ? object.issuer.map((e) => globalThis.String(e)) : [],
            startDate: isSet(object.startDate) ? fromJsonTimestamp(object.startDate) : undefined,
            endDate: isSet(object.endDate) ? fromJsonTimestamp(object.endDate) : undefined,
            scopeType: globalThis.Array.isArray(object?.scopeType)
                ? object.scopeType.map((e) => (0, ban_details_1.banScopeTypeFromJSON)(e))
                : [],
            scopeTarget: globalThis.Array.isArray(object?.scopeTarget)
                ? object.scopeTarget.map((e) => globalThis.String(e))
                : [],
            issuedDate: isSet(object.issuedDate) ? fromJsonTimestamp(object.issuedDate) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.targetType?.length) {
            obj.targetType = message.targetType.map((e) => (0, ban_details_1.banTargetTypeToJSON)(e));
        }
        if (message.target?.length) {
            obj.target = message.target;
        }
        if (message.issuer?.length) {
            obj.issuer = message.issuer;
        }
        if (message.startDate !== undefined) {
            obj.startDate = message.startDate.toISOString();
        }
        if (message.endDate !== undefined) {
            obj.endDate = message.endDate.toISOString();
        }
        if (message.scopeType?.length) {
            obj.scopeType = message.scopeType.map((e) => (0, ban_details_1.banScopeTypeToJSON)(e));
        }
        if (message.scopeTarget?.length) {
            obj.scopeTarget = message.scopeTarget;
        }
        if (message.issuedDate !== undefined) {
            obj.issuedDate = message.issuedDate.toISOString();
        }
        return obj;
    },
    create(base) {
        return exports.ListBansRequest_Filter.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListBansRequest_Filter();
        message.targetType = object.targetType?.map((e) => e) || [];
        message.target = object.target?.map((e) => e) || [];
        message.issuer = object.issuer?.map((e) => e) || [];
        message.startDate = object.startDate ?? undefined;
        message.endDate = object.endDate ?? undefined;
        message.scopeType = object.scopeType?.map((e) => e) || [];
        message.scopeTarget = object.scopeTarget?.map((e) => e) || [];
        message.issuedDate = object.issuedDate ?? undefined;
        return message;
    },
};
function createBaseListBansResponse() {
    return { page: undefined, items: [] };
}
exports.ListBansResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.page !== undefined) {
            page_1.PageResults.encode(message.page, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            ban_details_1.Ban.encode(v, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListBansResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.page = page_1.PageResults.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.items.push(ban_details_1.Ban.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            page: isSet(object.page) ? page_1.PageResults.fromJSON(object.page) : undefined,
            items: globalThis.Array.isArray(object?.items) ? object.items.map((e) => ban_details_1.Ban.fromJSON(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.page !== undefined) {
            obj.page = page_1.PageResults.toJSON(message.page);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => ban_details_1.Ban.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.ListBansResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListBansResponse();
        message.page = (object.page !== undefined && object.page !== null)
            ? page_1.PageResults.fromPartial(object.page)
            : undefined;
        message.items = object.items?.map((e) => ban_details_1.Ban.fromPartial(e)) || [];
        return message;
    },
};
function toTimestamp(date) {
    const seconds = BigInt(Math.trunc(date.getTime() / 1000));
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof globalThis.Date) {
        return o;
    }
    else if (typeof o === "string") {
        return new globalThis.Date(o);
    }
    else {
        return fromTimestamp(timestamp_1.Timestamp.fromJSON(o));
    }
}
function isSet(value) {
    return value !== null && value !== undefined;
}
