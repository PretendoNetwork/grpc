// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/list_ban_comments_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Comment } from "./comment";

export const protobufPackage = "account.v2";

export interface ListBanCommentsRequest {
  id: string;
}

export interface ListBanCommentsResponse {
  items: Comment[];
}

function createBaseListBanCommentsRequest(): ListBanCommentsRequest {
  return { id: "" };
}

export const ListBanCommentsRequest: MessageFns<ListBanCommentsRequest> = {
  encode(message: ListBanCommentsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBanCommentsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBanCommentsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBanCommentsRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: ListBanCommentsRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<ListBanCommentsRequest>): ListBanCommentsRequest {
    return ListBanCommentsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBanCommentsRequest>): ListBanCommentsRequest {
    const message = createBaseListBanCommentsRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseListBanCommentsResponse(): ListBanCommentsResponse {
  return { items: [] };
}

export const ListBanCommentsResponse: MessageFns<ListBanCommentsResponse> = {
  encode(message: ListBanCommentsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      Comment.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBanCommentsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBanCommentsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(Comment.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBanCommentsResponse {
    return { items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => Comment.fromJSON(e)) : [] };
  },

  toJSON(message: ListBanCommentsResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => Comment.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListBanCommentsResponse>): ListBanCommentsResponse {
    return ListBanCommentsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBanCommentsResponse>): ListBanCommentsResponse {
    const message = createBaseListBanCommentsResponse();
    message.items = object.items?.map((e) => Comment.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
