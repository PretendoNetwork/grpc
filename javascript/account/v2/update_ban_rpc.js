"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/update_ban_rpc.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.UpdateBanResponse = exports.UpdateBanRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const timestamp_1 = require("../../google/protobuf/timestamp");
const ban_details_1 = require("./ban_details");
exports.protobufPackage = "account.v2";
function createBaseUpdateBanRequest() {
    return {
        id: "",
        startDate: undefined,
        endDate: undefined,
        scopeType: undefined,
        scopeTarget: undefined,
        reason: undefined,
        pardonReason: undefined,
    };
}
exports.UpdateBanRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.id !== "") {
            writer.uint32(10).string(message.id);
        }
        if (message.startDate !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.startDate), writer.uint32(34).fork()).join();
        }
        if (message.endDate !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.endDate), writer.uint32(42).fork()).join();
        }
        if (message.scopeType !== undefined) {
            writer.uint32(48).int32(message.scopeType);
        }
        if (message.scopeTarget !== undefined) {
            writer.uint32(58).string(message.scopeTarget);
        }
        if (message.reason !== undefined) {
            writer.uint32(66).string(message.reason);
        }
        if (message.pardonReason !== undefined) {
            writer.uint32(74).string(message.pardonReason);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateBanRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.id = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.startDate = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.endDate = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.scopeType = reader.int32();
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.scopeTarget = reader.string();
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.reason = reader.string();
                    continue;
                }
                case 9: {
                    if (tag !== 74) {
                        break;
                    }
                    message.pardonReason = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            id: isSet(object.id) ? globalThis.String(object.id) : "",
            startDate: isSet(object.startDate) ? fromJsonTimestamp(object.startDate) : undefined,
            endDate: isSet(object.endDate) ? fromJsonTimestamp(object.endDate) : undefined,
            scopeType: isSet(object.scopeType) ? (0, ban_details_1.banScopeTypeFromJSON)(object.scopeType) : undefined,
            scopeTarget: isSet(object.scopeTarget) ? globalThis.String(object.scopeTarget) : undefined,
            reason: isSet(object.reason) ? globalThis.String(object.reason) : undefined,
            pardonReason: isSet(object.pardonReason) ? globalThis.String(object.pardonReason) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== "") {
            obj.id = message.id;
        }
        if (message.startDate !== undefined) {
            obj.startDate = message.startDate.toISOString();
        }
        if (message.endDate !== undefined) {
            obj.endDate = message.endDate.toISOString();
        }
        if (message.scopeType !== undefined) {
            obj.scopeType = (0, ban_details_1.banScopeTypeToJSON)(message.scopeType);
        }
        if (message.scopeTarget !== undefined) {
            obj.scopeTarget = message.scopeTarget;
        }
        if (message.reason !== undefined) {
            obj.reason = message.reason;
        }
        if (message.pardonReason !== undefined) {
            obj.pardonReason = message.pardonReason;
        }
        return obj;
    },
    create(base) {
        return exports.UpdateBanRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateBanRequest();
        message.id = object.id ?? "";
        message.startDate = object.startDate ?? undefined;
        message.endDate = object.endDate ?? undefined;
        message.scopeType = object.scopeType ?? undefined;
        message.scopeTarget = object.scopeTarget ?? undefined;
        message.reason = object.reason ?? undefined;
        message.pardonReason = object.pardonReason ?? undefined;
        return message;
    },
};
function createBaseUpdateBanResponse() {
    return { ban: undefined };
}
exports.UpdateBanResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.ban !== undefined) {
            ban_details_1.Ban.encode(message.ban, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseUpdateBanResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.ban = ban_details_1.Ban.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { ban: isSet(object.ban) ? ban_details_1.Ban.fromJSON(object.ban) : undefined };
    },
    toJSON(message) {
        const obj = {};
        if (message.ban !== undefined) {
            obj.ban = ban_details_1.Ban.toJSON(message.ban);
        }
        return obj;
    },
    create(base) {
        return exports.UpdateBanResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseUpdateBanResponse();
        message.ban = (object.ban !== undefined && object.ban !== null) ? ban_details_1.Ban.fromPartial(object.ban) : undefined;
        return message;
    },
};
function toTimestamp(date) {
    const seconds = BigInt(Math.trunc(date.getTime() / 1000));
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof globalThis.Date) {
        return o;
    }
    else if (typeof o === "string") {
        return new globalThis.Date(o);
    }
    else {
        return fromTimestamp(timestamp_1.Timestamp.fromJSON(o));
    }
}
function isSet(value) {
    return value !== null && value !== undefined;
}
