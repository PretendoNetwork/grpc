"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/list_devices_rpc.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.ListDevicesResponse = exports.ListDevicesRequest_Filter = exports.ListDevicesRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const device_1 = require("./device");
const page_1 = require("./page");
exports.protobufPackage = "account.v2";
function createBaseListDevicesRequest() {
    return { page: undefined, filter: undefined };
}
exports.ListDevicesRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.page !== undefined) {
            page_1.PageInput.encode(message.page, writer.uint32(10).fork()).join();
        }
        if (message.filter !== undefined) {
            exports.ListDevicesRequest_Filter.encode(message.filter, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListDevicesRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.page = page_1.PageInput.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.filter = exports.ListDevicesRequest_Filter.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            page: isSet(object.page) ? page_1.PageInput.fromJSON(object.page) : undefined,
            filter: isSet(object.filter) ? exports.ListDevicesRequest_Filter.fromJSON(object.filter) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.page !== undefined) {
            obj.page = page_1.PageInput.toJSON(message.page);
        }
        if (message.filter !== undefined) {
            obj.filter = exports.ListDevicesRequest_Filter.toJSON(message.filter);
        }
        return obj;
    },
    create(base) {
        return exports.ListDevicesRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListDevicesRequest();
        message.page = (object.page !== undefined && object.page !== null) ? page_1.PageInput.fromPartial(object.page) : undefined;
        message.filter = (object.filter !== undefined && object.filter !== null)
            ? exports.ListDevicesRequest_Filter.fromPartial(object.filter)
            : undefined;
        return message;
    },
};
function createBaseListDevicesRequest_Filter() {
    return { pid: [], deviceId: undefined, serial: undefined, extraJson: undefined };
}
exports.ListDevicesRequest_Filter = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        writer.uint32(10).fork();
        for (const v of message.pid) {
            writer.uint32(v);
        }
        writer.join();
        if (message.deviceId !== undefined) {
            writer.uint32(16).uint32(message.deviceId);
        }
        if (message.serial !== undefined) {
            writer.uint32(26).string(message.serial);
        }
        if (message.extraJson !== undefined) {
            writer.uint32(34).string(message.extraJson);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListDevicesRequest_Filter();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag === 8) {
                        message.pid.push(reader.uint32());
                        continue;
                    }
                    if (tag === 10) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.pid.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.deviceId = reader.uint32();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.serial = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.extraJson = reader.string();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            pid: globalThis.Array.isArray(object?.pid) ? object.pid.map((e) => globalThis.Number(e)) : [],
            deviceId: isSet(object.deviceId) ? globalThis.Number(object.deviceId) : undefined,
            serial: isSet(object.serial) ? globalThis.String(object.serial) : undefined,
            extraJson: isSet(object.extraJson) ? globalThis.String(object.extraJson) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.pid?.length) {
            obj.pid = message.pid.map((e) => Math.round(e));
        }
        if (message.deviceId !== undefined) {
            obj.deviceId = Math.round(message.deviceId);
        }
        if (message.serial !== undefined) {
            obj.serial = message.serial;
        }
        if (message.extraJson !== undefined) {
            obj.extraJson = message.extraJson;
        }
        return obj;
    },
    create(base) {
        return exports.ListDevicesRequest_Filter.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListDevicesRequest_Filter();
        message.pid = object.pid?.map((e) => e) || [];
        message.deviceId = object.deviceId ?? undefined;
        message.serial = object.serial ?? undefined;
        message.extraJson = object.extraJson ?? undefined;
        return message;
    },
};
function createBaseListDevicesResponse() {
    return { page: undefined, items: [] };
}
exports.ListDevicesResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.page !== undefined) {
            page_1.PageResults.encode(message.page, writer.uint32(10).fork()).join();
        }
        for (const v of message.items) {
            device_1.Device.encode(v, writer.uint32(18).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseListDevicesResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.page = page_1.PageResults.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.items.push(device_1.Device.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            page: isSet(object.page) ? page_1.PageResults.fromJSON(object.page) : undefined,
            items: globalThis.Array.isArray(object?.items) ? object.items.map((e) => device_1.Device.fromJSON(e)) : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.page !== undefined) {
            obj.page = page_1.PageResults.toJSON(message.page);
        }
        if (message.items?.length) {
            obj.items = message.items.map((e) => device_1.Device.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.ListDevicesResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseListDevicesResponse();
        message.page = (object.page !== undefined && object.page !== null)
            ? page_1.PageResults.fromPartial(object.page)
            : undefined;
        message.items = object.items?.map((e) => device_1.Device.fromPartial(e)) || [];
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
