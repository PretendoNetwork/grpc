// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/pardon_ban_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Ban } from "./ban_details";

export const protobufPackage = "account.v2";

export interface PardonBanRequest {
  id: string;
  reason: string;
}

export interface PardonBanResponse {
  ban: Ban | undefined;
}

function createBasePardonBanRequest(): PardonBanRequest {
  return { id: "", reason: "" };
}

export const PardonBanRequest: MessageFns<PardonBanRequest> = {
  encode(message: PardonBanRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PardonBanRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePardonBanRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PardonBanRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: PardonBanRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create(base?: DeepPartial<PardonBanRequest>): PardonBanRequest {
    return PardonBanRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PardonBanRequest>): PardonBanRequest {
    const message = createBasePardonBanRequest();
    message.id = object.id ?? "";
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBasePardonBanResponse(): PardonBanResponse {
  return { ban: undefined };
}

export const PardonBanResponse: MessageFns<PardonBanResponse> = {
  encode(message: PardonBanResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ban !== undefined) {
      Ban.encode(message.ban, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PardonBanResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePardonBanResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ban = Ban.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PardonBanResponse {
    return { ban: isSet(object.ban) ? Ban.fromJSON(object.ban) : undefined };
  },

  toJSON(message: PardonBanResponse): unknown {
    const obj: any = {};
    if (message.ban !== undefined) {
      obj.ban = Ban.toJSON(message.ban);
    }
    return obj;
  },

  create(base?: DeepPartial<PardonBanResponse>): PardonBanResponse {
    return PardonBanResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PardonBanResponse>): PardonBanResponse {
    const message = createBasePardonBanResponse();
    message.ban = (object.ban !== undefined && object.ban !== null) ? Ban.fromPartial(object.ban) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
