// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/list_audit_logs_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AuditLog } from "./audit_log";
import { AuditLogActionType, auditLogActionTypeFromJSON, auditLogActionTypeToJSON } from "./audit_log_action_type";
import { PageInput, PageResults } from "./page";

export const protobufPackage = "account.v2";

export interface ListAuditLogsRequest {
  page: PageInput | undefined;
  filter: ListAuditLogsRequest_Filter | undefined;
}

export interface ListAuditLogsRequest_Filter {
  executedByPid: number[];
  actionType: AuditLogActionType[];
  extraJson?: string | undefined;
}

export interface ListAuditLogsResponse {
  page: PageResults | undefined;
  items: AuditLog[];
}

function createBaseListAuditLogsRequest(): ListAuditLogsRequest {
  return { page: undefined, filter: undefined };
}

export const ListAuditLogsRequest: MessageFns<ListAuditLogsRequest> = {
  encode(message: ListAuditLogsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageInput.encode(message.page, writer.uint32(10).fork()).join();
    }
    if (message.filter !== undefined) {
      ListAuditLogsRequest_Filter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAuditLogsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAuditLogsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageInput.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = ListAuditLogsRequest_Filter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAuditLogsRequest {
    return {
      page: isSet(object.page) ? PageInput.fromJSON(object.page) : undefined,
      filter: isSet(object.filter) ? ListAuditLogsRequest_Filter.fromJSON(object.filter) : undefined,
    };
  },

  toJSON(message: ListAuditLogsRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageInput.toJSON(message.page);
    }
    if (message.filter !== undefined) {
      obj.filter = ListAuditLogsRequest_Filter.toJSON(message.filter);
    }
    return obj;
  },

  create(base?: DeepPartial<ListAuditLogsRequest>): ListAuditLogsRequest {
    return ListAuditLogsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListAuditLogsRequest>): ListAuditLogsRequest {
    const message = createBaseListAuditLogsRequest();
    message.page = (object.page !== undefined && object.page !== null) ? PageInput.fromPartial(object.page) : undefined;
    message.filter = (object.filter !== undefined && object.filter !== null)
      ? ListAuditLogsRequest_Filter.fromPartial(object.filter)
      : undefined;
    return message;
  },
};

function createBaseListAuditLogsRequest_Filter(): ListAuditLogsRequest_Filter {
  return { executedByPid: [], actionType: [], extraJson: undefined };
}

export const ListAuditLogsRequest_Filter: MessageFns<ListAuditLogsRequest_Filter> = {
  encode(message: ListAuditLogsRequest_Filter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.executedByPid) {
      writer.uint32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.actionType) {
      writer.int32(v);
    }
    writer.join();
    if (message.extraJson !== undefined) {
      writer.uint32(26).string(message.extraJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAuditLogsRequest_Filter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAuditLogsRequest_Filter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.executedByPid.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.executedByPid.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.actionType.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.actionType.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.extraJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAuditLogsRequest_Filter {
    return {
      executedByPid: globalThis.Array.isArray(object?.executedByPid)
        ? object.executedByPid.map((e: any) => globalThis.Number(e))
        : [],
      actionType: globalThis.Array.isArray(object?.actionType)
        ? object.actionType.map((e: any) => auditLogActionTypeFromJSON(e))
        : [],
      extraJson: isSet(object.extraJson) ? globalThis.String(object.extraJson) : undefined,
    };
  },

  toJSON(message: ListAuditLogsRequest_Filter): unknown {
    const obj: any = {};
    if (message.executedByPid?.length) {
      obj.executedByPid = message.executedByPid.map((e) => Math.round(e));
    }
    if (message.actionType?.length) {
      obj.actionType = message.actionType.map((e) => auditLogActionTypeToJSON(e));
    }
    if (message.extraJson !== undefined) {
      obj.extraJson = message.extraJson;
    }
    return obj;
  },

  create(base?: DeepPartial<ListAuditLogsRequest_Filter>): ListAuditLogsRequest_Filter {
    return ListAuditLogsRequest_Filter.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListAuditLogsRequest_Filter>): ListAuditLogsRequest_Filter {
    const message = createBaseListAuditLogsRequest_Filter();
    message.executedByPid = object.executedByPid?.map((e) => e) || [];
    message.actionType = object.actionType?.map((e) => e) || [];
    message.extraJson = object.extraJson ?? undefined;
    return message;
  },
};

function createBaseListAuditLogsResponse(): ListAuditLogsResponse {
  return { page: undefined, items: [] };
}

export const ListAuditLogsResponse: MessageFns<ListAuditLogsResponse> = {
  encode(message: ListAuditLogsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageResults.encode(message.page, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      AuditLog.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListAuditLogsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListAuditLogsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageResults.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(AuditLog.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListAuditLogsResponse {
    return {
      page: isSet(object.page) ? PageResults.fromJSON(object.page) : undefined,
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => AuditLog.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListAuditLogsResponse): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageResults.toJSON(message.page);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => AuditLog.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListAuditLogsResponse>): ListAuditLogsResponse {
    return ListAuditLogsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListAuditLogsResponse>): ListAuditLogsResponse {
    const message = createBaseListAuditLogsResponse();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResults.fromPartial(object.page)
      : undefined;
    message.items = object.items?.map((e) => AuditLog.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
