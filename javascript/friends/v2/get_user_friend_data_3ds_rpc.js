"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: friends/v2/get_user_friend_data_3ds_rpc.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.GetUserFriendsData3DSResponse = exports.GetUserFriendsData3DSRequest = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const friend_info_1 = require("./friend_info");
exports.protobufPackage = "friends.v2";
function createBaseGetUserFriendsData3DSRequest() {
    return { pid: 0 };
}
exports.GetUserFriendsData3DSRequest = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.pid !== 0) {
            writer.uint32(8).uint32(message.pid);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetUserFriendsData3DSRequest();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.pid = reader.uint32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return { pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0 };
    },
    toJSON(message) {
        const obj = {};
        if (message.pid !== 0) {
            obj.pid = Math.round(message.pid);
        }
        return obj;
    },
    create(base) {
        return exports.GetUserFriendsData3DSRequest.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetUserFriendsData3DSRequest();
        message.pid = object.pid ?? 0;
        return message;
    },
};
function createBaseGetUserFriendsData3DSResponse() {
    return { friends: [] };
}
exports.GetUserFriendsData3DSResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        for (const v of message.friends) {
            friend_info_1.FriendInfo3DS.encode(v, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGetUserFriendsData3DSResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.friends.push(friend_info_1.FriendInfo3DS.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            friends: globalThis.Array.isArray(object?.friends)
                ? object.friends.map((e) => friend_info_1.FriendInfo3DS.fromJSON(e))
                : [],
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.friends?.length) {
            obj.friends = message.friends.map((e) => friend_info_1.FriendInfo3DS.toJSON(e));
        }
        return obj;
    },
    create(base) {
        return exports.GetUserFriendsData3DSResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGetUserFriendsData3DSResponse();
        message.friends = object.friends?.map((e) => friend_info_1.FriendInfo3DS.fromPartial(e)) || [];
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
