"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: friends/v2/friend_info.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.FriendInfo3DS = exports.FriendInfoWiiU = exports.NintendoPresence = exports.NintendoPresenceV2 = exports.GameKey = exports.NNAInfo = exports.PrincipalBasicInfo = exports.MiiV2 = exports.FriendMii = exports.Mii = exports.Comment = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
const timestamp_1 = require("../../google/protobuf/timestamp");
exports.protobufPackage = "friends.v2";
function createBaseComment() {
    return { contents: "", lastChanged: undefined };
}
exports.Comment = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.contents !== "") {
            writer.uint32(18).string(message.contents);
        }
        if (message.lastChanged !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.lastChanged), writer.uint32(26).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseComment();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.contents = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.lastChanged = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            contents: isSet(object.contents) ? globalThis.String(object.contents) : "",
            lastChanged: isSet(object.lastChanged) ? fromJsonTimestamp(object.lastChanged) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.contents !== "") {
            obj.contents = message.contents;
        }
        if (message.lastChanged !== undefined) {
            obj.lastChanged = message.lastChanged.toISOString();
        }
        return obj;
    },
    create(base) {
        return exports.Comment.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseComment();
        message.contents = object.contents ?? "";
        message.lastChanged = object.lastChanged ?? undefined;
        return message;
    },
};
function createBaseMii() {
    return {
        name: "",
        profanityFlag: false,
        characterSet: 0,
        miiData: Buffer.alloc(0),
        miiDataEncrypted: Buffer.alloc(0),
    };
}
exports.Mii = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.name !== "") {
            writer.uint32(10).string(message.name);
        }
        if (message.profanityFlag !== false) {
            writer.uint32(16).bool(message.profanityFlag);
        }
        if (message.characterSet !== 0) {
            writer.uint32(24).uint32(message.characterSet);
        }
        if (message.miiData.length !== 0) {
            writer.uint32(34).bytes(message.miiData);
        }
        if (message.miiDataEncrypted.length !== 0) {
            writer.uint32(42).bytes(message.miiDataEncrypted);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMii();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.profanityFlag = reader.bool();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.characterSet = reader.uint32();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.miiData = Buffer.from(reader.bytes());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.miiDataEncrypted = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            profanityFlag: isSet(object.profanityFlag) ? globalThis.Boolean(object.profanityFlag) : false,
            characterSet: isSet(object.characterSet) ? globalThis.Number(object.characterSet) : 0,
            miiData: isSet(object.miiData) ? Buffer.from(bytesFromBase64(object.miiData)) : Buffer.alloc(0),
            miiDataEncrypted: isSet(object.miiDataEncrypted)
                ? Buffer.from(bytesFromBase64(object.miiDataEncrypted))
                : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.profanityFlag !== false) {
            obj.profanityFlag = message.profanityFlag;
        }
        if (message.characterSet !== 0) {
            obj.characterSet = Math.round(message.characterSet);
        }
        if (message.miiData.length !== 0) {
            obj.miiData = base64FromBytes(message.miiData);
        }
        if (message.miiDataEncrypted.length !== 0) {
            obj.miiDataEncrypted = base64FromBytes(message.miiDataEncrypted);
        }
        return obj;
    },
    create(base) {
        return exports.Mii.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseMii();
        message.name = object.name ?? "";
        message.profanityFlag = object.profanityFlag ?? false;
        message.characterSet = object.characterSet ?? 0;
        message.miiData = object.miiData ?? Buffer.alloc(0);
        message.miiDataEncrypted = object.miiDataEncrypted ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseFriendMii() {
    return { pid: 0, mii: undefined, modifiedAt: undefined };
}
exports.FriendMii = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.pid !== 0) {
            writer.uint32(8).uint32(message.pid);
        }
        if (message.mii !== undefined) {
            exports.Mii.encode(message.mii, writer.uint32(18).fork()).join();
        }
        if (message.modifiedAt !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.modifiedAt), writer.uint32(26).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFriendMii();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.pid = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.mii = exports.Mii.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.modifiedAt = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
            mii: isSet(object.mii) ? exports.Mii.fromJSON(object.mii) : undefined,
            modifiedAt: isSet(object.modifiedAt) ? fromJsonTimestamp(object.modifiedAt) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.pid !== 0) {
            obj.pid = Math.round(message.pid);
        }
        if (message.mii !== undefined) {
            obj.mii = exports.Mii.toJSON(message.mii);
        }
        if (message.modifiedAt !== undefined) {
            obj.modifiedAt = message.modifiedAt.toISOString();
        }
        return obj;
    },
    create(base) {
        return exports.FriendMii.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFriendMii();
        message.pid = object.pid ?? 0;
        message.mii = (object.mii !== undefined && object.mii !== null) ? exports.Mii.fromPartial(object.mii) : undefined;
        message.modifiedAt = object.modifiedAt ?? undefined;
        return message;
    },
};
function createBaseMiiV2() {
    return { name: "", miiData: Buffer.alloc(0), datetime: undefined };
}
exports.MiiV2 = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.name !== "") {
            writer.uint32(10).string(message.name);
        }
        if (message.miiData.length !== 0) {
            writer.uint32(34).bytes(message.miiData);
        }
        if (message.datetime !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.datetime), writer.uint32(42).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseMiiV2();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.name = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.miiData = Buffer.from(reader.bytes());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.datetime = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            name: isSet(object.name) ? globalThis.String(object.name) : "",
            miiData: isSet(object.miiData) ? Buffer.from(bytesFromBase64(object.miiData)) : Buffer.alloc(0),
            datetime: isSet(object.datetime) ? fromJsonTimestamp(object.datetime) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.name !== "") {
            obj.name = message.name;
        }
        if (message.miiData.length !== 0) {
            obj.miiData = base64FromBytes(message.miiData);
        }
        if (message.datetime !== undefined) {
            obj.datetime = message.datetime.toISOString();
        }
        return obj;
    },
    create(base) {
        return exports.MiiV2.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseMiiV2();
        message.name = object.name ?? "";
        message.miiData = object.miiData ?? Buffer.alloc(0);
        message.datetime = object.datetime ?? undefined;
        return message;
    },
};
function createBasePrincipalBasicInfo() {
    return { pid: 0, nnid: "", mii: undefined };
}
exports.PrincipalBasicInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.pid !== 0) {
            writer.uint32(8).uint32(message.pid);
        }
        if (message.nnid !== "") {
            writer.uint32(18).string(message.nnid);
        }
        if (message.mii !== undefined) {
            exports.MiiV2.encode(message.mii, writer.uint32(26).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePrincipalBasicInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.pid = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.nnid = reader.string();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.mii = exports.MiiV2.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
            nnid: isSet(object.nnid) ? globalThis.String(object.nnid) : "",
            mii: isSet(object.mii) ? exports.MiiV2.fromJSON(object.mii) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.pid !== 0) {
            obj.pid = Math.round(message.pid);
        }
        if (message.nnid !== "") {
            obj.nnid = message.nnid;
        }
        if (message.mii !== undefined) {
            obj.mii = exports.MiiV2.toJSON(message.mii);
        }
        return obj;
    },
    create(base) {
        return exports.PrincipalBasicInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBasePrincipalBasicInfo();
        message.pid = object.pid ?? 0;
        message.nnid = object.nnid ?? "";
        message.mii = (object.mii !== undefined && object.mii !== null) ? exports.MiiV2.fromPartial(object.mii) : undefined;
        return message;
    },
};
function createBaseNNAInfo() {
    return { principalBasicInfo: undefined };
}
exports.NNAInfo = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.principalBasicInfo !== undefined) {
            exports.PrincipalBasicInfo.encode(message.principalBasicInfo, writer.uint32(10).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseNNAInfo();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.principalBasicInfo = exports.PrincipalBasicInfo.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            principalBasicInfo: isSet(object.principalBasicInfo)
                ? exports.PrincipalBasicInfo.fromJSON(object.principalBasicInfo)
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.principalBasicInfo !== undefined) {
            obj.principalBasicInfo = exports.PrincipalBasicInfo.toJSON(message.principalBasicInfo);
        }
        return obj;
    },
    create(base) {
        return exports.NNAInfo.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseNNAInfo();
        message.principalBasicInfo = (object.principalBasicInfo !== undefined && object.principalBasicInfo !== null)
            ? exports.PrincipalBasicInfo.fromPartial(object.principalBasicInfo)
            : undefined;
        return message;
    },
};
function createBaseGameKey() {
    return { titleId: 0n, titleVersion: 0 };
}
exports.GameKey = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.titleId !== 0n) {
            if (BigInt.asUintN(64, message.titleId) !== message.titleId) {
                throw new globalThis.Error("value provided for field message.titleId of type uint64 too large");
            }
            writer.uint32(8).uint64(message.titleId);
        }
        if (message.titleVersion !== 0) {
            writer.uint32(16).uint32(message.titleVersion);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseGameKey();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.titleId = reader.uint64();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.titleVersion = reader.uint32();
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            titleId: isSet(object.titleId) ? BigInt(object.titleId) : 0n,
            titleVersion: isSet(object.titleVersion) ? globalThis.Number(object.titleVersion) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.titleId !== 0n) {
            obj.titleId = message.titleId.toString();
        }
        if (message.titleVersion !== 0) {
            obj.titleVersion = Math.round(message.titleVersion);
        }
        return obj;
    },
    create(base) {
        return exports.GameKey.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGameKey();
        message.titleId = object.titleId ?? 0n;
        message.titleVersion = object.titleVersion ?? 0;
        return message;
    },
};
function createBaseNintendoPresenceV2() {
    return {
        changedFlags: 0,
        online: false,
        gameKey: undefined,
        message: "",
        gameServerId: 0,
        pid: 0,
        gatheringId: 0,
        applicationData: Buffer.alloc(0),
    };
}
exports.NintendoPresenceV2 = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.changedFlags !== 0) {
            writer.uint32(8).uint32(message.changedFlags);
        }
        if (message.online !== false) {
            writer.uint32(16).bool(message.online);
        }
        if (message.gameKey !== undefined) {
            exports.GameKey.encode(message.gameKey, writer.uint32(26).fork()).join();
        }
        if (message.message !== "") {
            writer.uint32(42).string(message.message);
        }
        if (message.gameServerId !== 0) {
            writer.uint32(64).uint32(message.gameServerId);
        }
        if (message.pid !== 0) {
            writer.uint32(80).uint32(message.pid);
        }
        if (message.gatheringId !== 0) {
            writer.uint32(88).uint32(message.gatheringId);
        }
        if (message.applicationData.length !== 0) {
            writer.uint32(98).bytes(message.applicationData);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseNintendoPresenceV2();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.changedFlags = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.online = reader.bool();
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.gameKey = exports.GameKey.decode(reader, reader.uint32());
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.message = reader.string();
                    continue;
                }
                case 8: {
                    if (tag !== 64) {
                        break;
                    }
                    message.gameServerId = reader.uint32();
                    continue;
                }
                case 10: {
                    if (tag !== 80) {
                        break;
                    }
                    message.pid = reader.uint32();
                    continue;
                }
                case 11: {
                    if (tag !== 88) {
                        break;
                    }
                    message.gatheringId = reader.uint32();
                    continue;
                }
                case 12: {
                    if (tag !== 98) {
                        break;
                    }
                    message.applicationData = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            changedFlags: isSet(object.changedFlags) ? globalThis.Number(object.changedFlags) : 0,
            online: isSet(object.online) ? globalThis.Boolean(object.online) : false,
            gameKey: isSet(object.gameKey) ? exports.GameKey.fromJSON(object.gameKey) : undefined,
            message: isSet(object.message) ? globalThis.String(object.message) : "",
            gameServerId: isSet(object.gameServerId) ? globalThis.Number(object.gameServerId) : 0,
            pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
            gatheringId: isSet(object.gatheringId) ? globalThis.Number(object.gatheringId) : 0,
            applicationData: isSet(object.applicationData)
                ? Buffer.from(bytesFromBase64(object.applicationData))
                : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.changedFlags !== 0) {
            obj.changedFlags = Math.round(message.changedFlags);
        }
        if (message.online !== false) {
            obj.online = message.online;
        }
        if (message.gameKey !== undefined) {
            obj.gameKey = exports.GameKey.toJSON(message.gameKey);
        }
        if (message.message !== "") {
            obj.message = message.message;
        }
        if (message.gameServerId !== 0) {
            obj.gameServerId = Math.round(message.gameServerId);
        }
        if (message.pid !== 0) {
            obj.pid = Math.round(message.pid);
        }
        if (message.gatheringId !== 0) {
            obj.gatheringId = Math.round(message.gatheringId);
        }
        if (message.applicationData.length !== 0) {
            obj.applicationData = base64FromBytes(message.applicationData);
        }
        return obj;
    },
    create(base) {
        return exports.NintendoPresenceV2.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseNintendoPresenceV2();
        message.changedFlags = object.changedFlags ?? 0;
        message.online = object.online ?? false;
        message.gameKey = (object.gameKey !== undefined && object.gameKey !== null)
            ? exports.GameKey.fromPartial(object.gameKey)
            : undefined;
        message.message = object.message ?? "";
        message.gameServerId = object.gameServerId ?? 0;
        message.pid = object.pid ?? 0;
        message.gatheringId = object.gatheringId ?? 0;
        message.applicationData = object.applicationData ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseNintendoPresence() {
    return {
        changedFlags: 0,
        gameKey: undefined,
        message: "",
        joinAvailableFlag: 0,
        matchmakeType: 0,
        joinGameId: 0,
        joinGameMode: 0,
        ownerPid: 0,
        joinGroupId: 0,
        applicationArg: Buffer.alloc(0),
    };
}
exports.NintendoPresence = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.changedFlags !== 0) {
            writer.uint32(8).uint32(message.changedFlags);
        }
        if (message.gameKey !== undefined) {
            exports.GameKey.encode(message.gameKey, writer.uint32(18).fork()).join();
        }
        if (message.message !== "") {
            writer.uint32(26).string(message.message);
        }
        if (message.joinAvailableFlag !== 0) {
            writer.uint32(32).uint32(message.joinAvailableFlag);
        }
        if (message.matchmakeType !== 0) {
            writer.uint32(40).uint32(message.matchmakeType);
        }
        if (message.joinGameId !== 0) {
            writer.uint32(48).uint32(message.joinGameId);
        }
        if (message.joinGameMode !== 0) {
            writer.uint32(56).uint32(message.joinGameMode);
        }
        if (message.ownerPid !== 0) {
            writer.uint32(64).uint32(message.ownerPid);
        }
        if (message.joinGroupId !== 0) {
            writer.uint32(72).uint32(message.joinGroupId);
        }
        if (message.applicationArg.length !== 0) {
            writer.uint32(82).bytes(message.applicationArg);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseNintendoPresence();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.changedFlags = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.gameKey = exports.GameKey.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.message = reader.string();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.joinAvailableFlag = reader.uint32();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.matchmakeType = reader.uint32();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.joinGameId = reader.uint32();
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.joinGameMode = reader.uint32();
                    continue;
                }
                case 8: {
                    if (tag !== 64) {
                        break;
                    }
                    message.ownerPid = reader.uint32();
                    continue;
                }
                case 9: {
                    if (tag !== 72) {
                        break;
                    }
                    message.joinGroupId = reader.uint32();
                    continue;
                }
                case 10: {
                    if (tag !== 82) {
                        break;
                    }
                    message.applicationArg = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            changedFlags: isSet(object.changedFlags) ? globalThis.Number(object.changedFlags) : 0,
            gameKey: isSet(object.gameKey) ? exports.GameKey.fromJSON(object.gameKey) : undefined,
            message: isSet(object.message) ? globalThis.String(object.message) : "",
            joinAvailableFlag: isSet(object.joinAvailableFlag) ? globalThis.Number(object.joinAvailableFlag) : 0,
            matchmakeType: isSet(object.matchmakeType) ? globalThis.Number(object.matchmakeType) : 0,
            joinGameId: isSet(object.joinGameId) ? globalThis.Number(object.joinGameId) : 0,
            joinGameMode: isSet(object.joinGameMode) ? globalThis.Number(object.joinGameMode) : 0,
            ownerPid: isSet(object.ownerPid) ? globalThis.Number(object.ownerPid) : 0,
            joinGroupId: isSet(object.joinGroupId) ? globalThis.Number(object.joinGroupId) : 0,
            applicationArg: isSet(object.applicationArg)
                ? Buffer.from(bytesFromBase64(object.applicationArg))
                : Buffer.alloc(0),
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.changedFlags !== 0) {
            obj.changedFlags = Math.round(message.changedFlags);
        }
        if (message.gameKey !== undefined) {
            obj.gameKey = exports.GameKey.toJSON(message.gameKey);
        }
        if (message.message !== "") {
            obj.message = message.message;
        }
        if (message.joinAvailableFlag !== 0) {
            obj.joinAvailableFlag = Math.round(message.joinAvailableFlag);
        }
        if (message.matchmakeType !== 0) {
            obj.matchmakeType = Math.round(message.matchmakeType);
        }
        if (message.joinGameId !== 0) {
            obj.joinGameId = Math.round(message.joinGameId);
        }
        if (message.joinGameMode !== 0) {
            obj.joinGameMode = Math.round(message.joinGameMode);
        }
        if (message.ownerPid !== 0) {
            obj.ownerPid = Math.round(message.ownerPid);
        }
        if (message.joinGroupId !== 0) {
            obj.joinGroupId = Math.round(message.joinGroupId);
        }
        if (message.applicationArg.length !== 0) {
            obj.applicationArg = base64FromBytes(message.applicationArg);
        }
        return obj;
    },
    create(base) {
        return exports.NintendoPresence.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseNintendoPresence();
        message.changedFlags = object.changedFlags ?? 0;
        message.gameKey = (object.gameKey !== undefined && object.gameKey !== null)
            ? exports.GameKey.fromPartial(object.gameKey)
            : undefined;
        message.message = object.message ?? "";
        message.joinAvailableFlag = object.joinAvailableFlag ?? 0;
        message.matchmakeType = object.matchmakeType ?? 0;
        message.joinGameId = object.joinGameId ?? 0;
        message.joinGameMode = object.joinGameMode ?? 0;
        message.ownerPid = object.ownerPid ?? 0;
        message.joinGroupId = object.joinGroupId ?? 0;
        message.applicationArg = object.applicationArg ?? Buffer.alloc(0);
        return message;
    },
};
function createBaseFriendInfoWiiU() {
    return { nnaInfo: undefined, presence: undefined, status: undefined, becameFriend: undefined, lastOnline: undefined };
}
exports.FriendInfoWiiU = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.nnaInfo !== undefined) {
            exports.NNAInfo.encode(message.nnaInfo, writer.uint32(10).fork()).join();
        }
        if (message.presence !== undefined) {
            exports.NintendoPresenceV2.encode(message.presence, writer.uint32(18).fork()).join();
        }
        if (message.status !== undefined) {
            exports.Comment.encode(message.status, writer.uint32(26).fork()).join();
        }
        if (message.becameFriend !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.becameFriend), writer.uint32(34).fork()).join();
        }
        if (message.lastOnline !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.lastOnline), writer.uint32(42).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFriendInfoWiiU();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 10) {
                        break;
                    }
                    message.nnaInfo = exports.NNAInfo.decode(reader, reader.uint32());
                    continue;
                }
                case 2: {
                    if (tag !== 18) {
                        break;
                    }
                    message.presence = exports.NintendoPresenceV2.decode(reader, reader.uint32());
                    continue;
                }
                case 3: {
                    if (tag !== 26) {
                        break;
                    }
                    message.status = exports.Comment.decode(reader, reader.uint32());
                    continue;
                }
                case 4: {
                    if (tag !== 34) {
                        break;
                    }
                    message.becameFriend = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 5: {
                    if (tag !== 42) {
                        break;
                    }
                    message.lastOnline = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            nnaInfo: isSet(object.nnaInfo) ? exports.NNAInfo.fromJSON(object.nnaInfo) : undefined,
            presence: isSet(object.presence) ? exports.NintendoPresenceV2.fromJSON(object.presence) : undefined,
            status: isSet(object.status) ? exports.Comment.fromJSON(object.status) : undefined,
            becameFriend: isSet(object.becameFriend) ? fromJsonTimestamp(object.becameFriend) : undefined,
            lastOnline: isSet(object.lastOnline) ? fromJsonTimestamp(object.lastOnline) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.nnaInfo !== undefined) {
            obj.nnaInfo = exports.NNAInfo.toJSON(message.nnaInfo);
        }
        if (message.presence !== undefined) {
            obj.presence = exports.NintendoPresenceV2.toJSON(message.presence);
        }
        if (message.status !== undefined) {
            obj.status = exports.Comment.toJSON(message.status);
        }
        if (message.becameFriend !== undefined) {
            obj.becameFriend = message.becameFriend.toISOString();
        }
        if (message.lastOnline !== undefined) {
            obj.lastOnline = message.lastOnline.toISOString();
        }
        return obj;
    },
    create(base) {
        return exports.FriendInfoWiiU.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFriendInfoWiiU();
        message.nnaInfo = (object.nnaInfo !== undefined && object.nnaInfo !== null)
            ? exports.NNAInfo.fromPartial(object.nnaInfo)
            : undefined;
        message.presence = (object.presence !== undefined && object.presence !== null)
            ? exports.NintendoPresenceV2.fromPartial(object.presence)
            : undefined;
        message.status = (object.status !== undefined && object.status !== null)
            ? exports.Comment.fromPartial(object.status)
            : undefined;
        message.becameFriend = object.becameFriend ?? undefined;
        message.lastOnline = object.lastOnline ?? undefined;
        return message;
    },
};
function createBaseFriendInfo3DS() {
    return {
        pid: 0,
        region: 0,
        country: 0,
        area: 0,
        language: 0,
        platform: 0,
        presence: undefined,
        gameKey: undefined,
        message: "",
        messageUpdatedAt: undefined,
        miiModifiedAt: undefined,
        lastOnline: undefined,
        mii: undefined,
    };
}
exports.FriendInfo3DS = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.pid !== 0) {
            writer.uint32(8).uint32(message.pid);
        }
        if (message.region !== 0) {
            writer.uint32(16).uint32(message.region);
        }
        if (message.country !== 0) {
            writer.uint32(24).uint32(message.country);
        }
        if (message.area !== 0) {
            writer.uint32(32).uint32(message.area);
        }
        if (message.language !== 0) {
            writer.uint32(40).uint32(message.language);
        }
        if (message.platform !== 0) {
            writer.uint32(48).uint32(message.platform);
        }
        if (message.presence !== undefined) {
            exports.NintendoPresence.encode(message.presence, writer.uint32(58).fork()).join();
        }
        if (message.gameKey !== undefined) {
            exports.GameKey.encode(message.gameKey, writer.uint32(66).fork()).join();
        }
        if (message.message !== "") {
            writer.uint32(74).string(message.message);
        }
        if (message.messageUpdatedAt !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.messageUpdatedAt), writer.uint32(82).fork()).join();
        }
        if (message.miiModifiedAt !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.miiModifiedAt), writer.uint32(90).fork()).join();
        }
        if (message.lastOnline !== undefined) {
            timestamp_1.Timestamp.encode(toTimestamp(message.lastOnline), writer.uint32(98).fork()).join();
        }
        if (message.mii !== undefined) {
            exports.FriendMii.encode(message.mii, writer.uint32(106).fork()).join();
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseFriendInfo3DS();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.pid = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.region = reader.uint32();
                    continue;
                }
                case 3: {
                    if (tag !== 24) {
                        break;
                    }
                    message.country = reader.uint32();
                    continue;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.area = reader.uint32();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.language = reader.uint32();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.platform = reader.uint32();
                    continue;
                }
                case 7: {
                    if (tag !== 58) {
                        break;
                    }
                    message.presence = exports.NintendoPresence.decode(reader, reader.uint32());
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.gameKey = exports.GameKey.decode(reader, reader.uint32());
                    continue;
                }
                case 9: {
                    if (tag !== 74) {
                        break;
                    }
                    message.message = reader.string();
                    continue;
                }
                case 10: {
                    if (tag !== 82) {
                        break;
                    }
                    message.messageUpdatedAt = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 11: {
                    if (tag !== 90) {
                        break;
                    }
                    message.miiModifiedAt = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 12: {
                    if (tag !== 98) {
                        break;
                    }
                    message.lastOnline = fromTimestamp(timestamp_1.Timestamp.decode(reader, reader.uint32()));
                    continue;
                }
                case 13: {
                    if (tag !== 106) {
                        break;
                    }
                    message.mii = exports.FriendMii.decode(reader, reader.uint32());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
            region: isSet(object.region) ? globalThis.Number(object.region) : 0,
            country: isSet(object.country) ? globalThis.Number(object.country) : 0,
            area: isSet(object.area) ? globalThis.Number(object.area) : 0,
            language: isSet(object.language) ? globalThis.Number(object.language) : 0,
            platform: isSet(object.platform) ? globalThis.Number(object.platform) : 0,
            presence: isSet(object.presence) ? exports.NintendoPresence.fromJSON(object.presence) : undefined,
            gameKey: isSet(object.gameKey) ? exports.GameKey.fromJSON(object.gameKey) : undefined,
            message: isSet(object.message) ? globalThis.String(object.message) : "",
            messageUpdatedAt: isSet(object.messageUpdatedAt) ? fromJsonTimestamp(object.messageUpdatedAt) : undefined,
            miiModifiedAt: isSet(object.miiModifiedAt) ? fromJsonTimestamp(object.miiModifiedAt) : undefined,
            lastOnline: isSet(object.lastOnline) ? fromJsonTimestamp(object.lastOnline) : undefined,
            mii: isSet(object.mii) ? exports.FriendMii.fromJSON(object.mii) : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.pid !== 0) {
            obj.pid = Math.round(message.pid);
        }
        if (message.region !== 0) {
            obj.region = Math.round(message.region);
        }
        if (message.country !== 0) {
            obj.country = Math.round(message.country);
        }
        if (message.area !== 0) {
            obj.area = Math.round(message.area);
        }
        if (message.language !== 0) {
            obj.language = Math.round(message.language);
        }
        if (message.platform !== 0) {
            obj.platform = Math.round(message.platform);
        }
        if (message.presence !== undefined) {
            obj.presence = exports.NintendoPresence.toJSON(message.presence);
        }
        if (message.gameKey !== undefined) {
            obj.gameKey = exports.GameKey.toJSON(message.gameKey);
        }
        if (message.message !== "") {
            obj.message = message.message;
        }
        if (message.messageUpdatedAt !== undefined) {
            obj.messageUpdatedAt = message.messageUpdatedAt.toISOString();
        }
        if (message.miiModifiedAt !== undefined) {
            obj.miiModifiedAt = message.miiModifiedAt.toISOString();
        }
        if (message.lastOnline !== undefined) {
            obj.lastOnline = message.lastOnline.toISOString();
        }
        if (message.mii !== undefined) {
            obj.mii = exports.FriendMii.toJSON(message.mii);
        }
        return obj;
    },
    create(base) {
        return exports.FriendInfo3DS.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseFriendInfo3DS();
        message.pid = object.pid ?? 0;
        message.region = object.region ?? 0;
        message.country = object.country ?? 0;
        message.area = object.area ?? 0;
        message.language = object.language ?? 0;
        message.platform = object.platform ?? 0;
        message.presence = (object.presence !== undefined && object.presence !== null)
            ? exports.NintendoPresence.fromPartial(object.presence)
            : undefined;
        message.gameKey = (object.gameKey !== undefined && object.gameKey !== null)
            ? exports.GameKey.fromPartial(object.gameKey)
            : undefined;
        message.message = object.message ?? "";
        message.messageUpdatedAt = object.messageUpdatedAt ?? undefined;
        message.miiModifiedAt = object.miiModifiedAt ?? undefined;
        message.lastOnline = object.lastOnline ?? undefined;
        message.mii = (object.mii !== undefined && object.mii !== null) ? exports.FriendMii.fromPartial(object.mii) : undefined;
        return message;
    },
};
function bytesFromBase64(b64) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}
function base64FromBytes(arr) {
    return globalThis.Buffer.from(arr).toString("base64");
}
function toTimestamp(date) {
    const seconds = BigInt(Math.trunc(date.getTime() / 1000));
    const nanos = (date.getTime() % 1000) * 1000000;
    return { seconds, nanos };
}
function fromTimestamp(t) {
    let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1000;
    millis += (t.nanos || 0) / 1000000;
    return new globalThis.Date(millis);
}
function fromJsonTimestamp(o) {
    if (o instanceof globalThis.Date) {
        return o;
    }
    else if (typeof o === "string") {
        return new globalThis.Date(o);
    }
    else {
        return fromTimestamp(timestamp_1.Timestamp.fromJSON(o));
    }
}
function isSet(value) {
    return value !== null && value !== undefined;
}
