// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: nex/v1/kill_user_connection_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "nex.v1";

export interface KillUserConnectionRequest {
  pid: bigint;
}

export interface KillUserConnectionResponse {
}

function createBaseKillUserConnectionRequest(): KillUserConnectionRequest {
  return { pid: 0n };
}

export const KillUserConnectionRequest: MessageFns<KillUserConnectionRequest> = {
  encode(message: KillUserConnectionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0n) {
      if (BigInt.asUintN(64, message.pid) !== message.pid) {
        throw new globalThis.Error("value provided for field message.pid of type uint64 too large");
      }
      writer.uint32(8).uint64(message.pid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillUserConnectionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillUserConnectionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KillUserConnectionRequest {
    return { pid: isSet(object.pid) ? BigInt(object.pid) : 0n };
  },

  toJSON(message: KillUserConnectionRequest): unknown {
    const obj: any = {};
    if (message.pid !== 0n) {
      obj.pid = message.pid.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<KillUserConnectionRequest>): KillUserConnectionRequest {
    return KillUserConnectionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KillUserConnectionRequest>): KillUserConnectionRequest {
    const message = createBaseKillUserConnectionRequest();
    message.pid = object.pid ?? 0n;
    return message;
  },
};

function createBaseKillUserConnectionResponse(): KillUserConnectionResponse {
  return {};
}

export const KillUserConnectionResponse: MessageFns<KillUserConnectionResponse> = {
  encode(_: KillUserConnectionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KillUserConnectionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKillUserConnectionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): KillUserConnectionResponse {
    return {};
  },

  toJSON(_: KillUserConnectionResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<KillUserConnectionResponse>): KillUserConnectionResponse {
    return KillUserConnectionResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<KillUserConnectionResponse>): KillUserConnectionResponse {
    const message = createBaseKillUserConnectionResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
