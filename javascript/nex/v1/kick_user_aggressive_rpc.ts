// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: nex/v1/kick_user_aggressive_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "nex.v1";

export interface KickUserAggressiveRequest {
  pid: bigint;
}

export interface KickUserAggressiveResponse {
}

function createBaseKickUserAggressiveRequest(): KickUserAggressiveRequest {
  return { pid: 0n };
}

export const KickUserAggressiveRequest: MessageFns<KickUserAggressiveRequest> = {
  encode(message: KickUserAggressiveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0n) {
      if (BigInt.asUintN(64, message.pid) !== message.pid) {
        throw new globalThis.Error("value provided for field message.pid of type uint64 too large");
      }
      writer.uint32(8).uint64(message.pid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KickUserAggressiveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKickUserAggressiveRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KickUserAggressiveRequest {
    return { pid: isSet(object.pid) ? BigInt(object.pid) : 0n };
  },

  toJSON(message: KickUserAggressiveRequest): unknown {
    const obj: any = {};
    if (message.pid !== 0n) {
      obj.pid = message.pid.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<KickUserAggressiveRequest>): KickUserAggressiveRequest {
    return KickUserAggressiveRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KickUserAggressiveRequest>): KickUserAggressiveRequest {
    const message = createBaseKickUserAggressiveRequest();
    message.pid = object.pid ?? 0n;
    return message;
  },
};

function createBaseKickUserAggressiveResponse(): KickUserAggressiveResponse {
  return {};
}

export const KickUserAggressiveResponse: MessageFns<KickUserAggressiveResponse> = {
  encode(_: KickUserAggressiveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KickUserAggressiveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKickUserAggressiveResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): KickUserAggressiveResponse {
    return {};
  },

  toJSON(_: KickUserAggressiveResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<KickUserAggressiveResponse>): KickUserAggressiveResponse {
    return KickUserAggressiveResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<KickUserAggressiveResponse>): KickUserAggressiveResponse {
    const message = createBaseKickUserAggressiveResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
