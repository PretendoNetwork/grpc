// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: nex/v1/kick_user_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "nex.v1";

export interface KickUserRequest {
  pid: bigint;
}

export interface KickUserResponse {
}

function createBaseKickUserRequest(): KickUserRequest {
  return { pid: 0n };
}

export const KickUserRequest: MessageFns<KickUserRequest> = {
  encode(message: KickUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0n) {
      if (BigInt.asUintN(64, message.pid) !== message.pid) {
        throw new globalThis.Error("value provided for field message.pid of type uint64 too large");
      }
      writer.uint32(8).uint64(message.pid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KickUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKickUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): KickUserRequest {
    return { pid: isSet(object.pid) ? BigInt(object.pid) : 0n };
  },

  toJSON(message: KickUserRequest): unknown {
    const obj: any = {};
    if (message.pid !== 0n) {
      obj.pid = message.pid.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<KickUserRequest>): KickUserRequest {
    return KickUserRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<KickUserRequest>): KickUserRequest {
    const message = createBaseKickUserRequest();
    message.pid = object.pid ?? 0n;
    return message;
  },
};

function createBaseKickUserResponse(): KickUserResponse {
  return {};
}

export const KickUserResponse: MessageFns<KickUserResponse> = {
  encode(_: KickUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): KickUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseKickUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): KickUserResponse {
    return {};
  },

  toJSON(_: KickUserResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<KickUserResponse>): KickUserResponse {
    return KickUserResponse.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<KickUserResponse>): KickUserResponse {
    const message = createBaseKickUserResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
