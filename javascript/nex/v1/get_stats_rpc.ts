// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: nex/v1/get_stats_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "nex.v1";

export interface GetStatsRequest {
}

export interface GetStatsResponse {
  playerCount: bigint;
}

function createBaseGetStatsRequest(): GetStatsRequest {
  return {};
}

export const GetStatsRequest: MessageFns<GetStatsRequest> = {
  encode(_: GetStatsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStatsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStatsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetStatsRequest {
    return {};
  },

  toJSON(_: GetStatsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetStatsRequest>): GetStatsRequest {
    return GetStatsRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetStatsRequest>): GetStatsRequest {
    const message = createBaseGetStatsRequest();
    return message;
  },
};

function createBaseGetStatsResponse(): GetStatsResponse {
  return { playerCount: 0n };
}

export const GetStatsResponse: MessageFns<GetStatsResponse> = {
  encode(message: GetStatsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerCount !== 0n) {
      if (BigInt.asUintN(64, message.playerCount) !== message.playerCount) {
        throw new globalThis.Error("value provided for field message.playerCount of type uint64 too large");
      }
      writer.uint32(8).uint64(message.playerCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStatsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStatsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.playerCount = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStatsResponse {
    return { playerCount: isSet(object.playerCount) ? BigInt(object.playerCount) : 0n };
  },

  toJSON(message: GetStatsResponse): unknown {
    const obj: any = {};
    if (message.playerCount !== 0n) {
      obj.playerCount = message.playerCount.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<GetStatsResponse>): GetStatsResponse {
    return GetStatsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetStatsResponse>): GetStatsResponse {
    const message = createBaseGetStatsResponse();
    message.playerCount = object.playerCount ?? 0n;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
