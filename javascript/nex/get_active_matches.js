"use strict";
// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: nex/get_active_matches.proto
Object.defineProperty(exports, "__esModule", { value: true });
exports.ActiveMatchesResponse = exports.protobufPackage = void 0;
/* eslint-disable */
const wire_1 = require("@bufbuild/protobuf/wire");
exports.protobufPackage = "nex";
function createBaseActiveMatchesResponse() {
    return {
        id: 0,
        startTime: 0n,
        participants: [],
        ownerPid: 0,
        hostPid: 0,
        gameMode: 0n,
        flags: 0n,
        applicationBuffer: undefined,
    };
}
exports.ActiveMatchesResponse = {
    encode(message, writer = new wire_1.BinaryWriter()) {
        if (message.id !== 0) {
            writer.uint32(8).uint32(message.id);
        }
        if (message.startTime !== 0n) {
            if (BigInt.asUintN(64, message.startTime) !== message.startTime) {
                throw new globalThis.Error("value provided for field message.startTime of type uint64 too large");
            }
            writer.uint32(16).uint64(message.startTime);
        }
        writer.uint32(26).fork();
        for (const v of message.participants) {
            writer.uint32(v);
        }
        writer.join();
        if (message.ownerPid !== 0) {
            writer.uint32(32).uint32(message.ownerPid);
        }
        if (message.hostPid !== 0) {
            writer.uint32(40).uint32(message.hostPid);
        }
        if (message.gameMode !== 0n) {
            if (BigInt.asUintN(64, message.gameMode) !== message.gameMode) {
                throw new globalThis.Error("value provided for field message.gameMode of type uint64 too large");
            }
            writer.uint32(48).uint64(message.gameMode);
        }
        if (message.flags !== 0n) {
            if (BigInt.asUintN(64, message.flags) !== message.flags) {
                throw new globalThis.Error("value provided for field message.flags of type uint64 too large");
            }
            writer.uint32(56).uint64(message.flags);
        }
        if (message.applicationBuffer !== undefined) {
            writer.uint32(66).bytes(message.applicationBuffer);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof wire_1.BinaryReader ? input : new wire_1.BinaryReader(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBaseActiveMatchesResponse();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1: {
                    if (tag !== 8) {
                        break;
                    }
                    message.id = reader.uint32();
                    continue;
                }
                case 2: {
                    if (tag !== 16) {
                        break;
                    }
                    message.startTime = reader.uint64();
                    continue;
                }
                case 3: {
                    if (tag === 24) {
                        message.participants.push(reader.uint32());
                        continue;
                    }
                    if (tag === 26) {
                        const end2 = reader.uint32() + reader.pos;
                        while (reader.pos < end2) {
                            message.participants.push(reader.uint32());
                        }
                        continue;
                    }
                    break;
                }
                case 4: {
                    if (tag !== 32) {
                        break;
                    }
                    message.ownerPid = reader.uint32();
                    continue;
                }
                case 5: {
                    if (tag !== 40) {
                        break;
                    }
                    message.hostPid = reader.uint32();
                    continue;
                }
                case 6: {
                    if (tag !== 48) {
                        break;
                    }
                    message.gameMode = reader.uint64();
                    continue;
                }
                case 7: {
                    if (tag !== 56) {
                        break;
                    }
                    message.flags = reader.uint64();
                    continue;
                }
                case 8: {
                    if (tag !== 66) {
                        break;
                    }
                    message.applicationBuffer = Buffer.from(reader.bytes());
                    continue;
                }
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skip(tag & 7);
        }
        return message;
    },
    fromJSON(object) {
        return {
            id: isSet(object.id) ? globalThis.Number(object.id) : 0,
            startTime: isSet(object.startTime) ? BigInt(object.startTime) : 0n,
            participants: globalThis.Array.isArray(object?.participants)
                ? object.participants.map((e) => globalThis.Number(e))
                : [],
            ownerPid: isSet(object.ownerPid) ? globalThis.Number(object.ownerPid) : 0,
            hostPid: isSet(object.hostPid) ? globalThis.Number(object.hostPid) : 0,
            gameMode: isSet(object.gameMode) ? BigInt(object.gameMode) : 0n,
            flags: isSet(object.flags) ? BigInt(object.flags) : 0n,
            applicationBuffer: isSet(object.applicationBuffer)
                ? Buffer.from(bytesFromBase64(object.applicationBuffer))
                : undefined,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.id !== 0) {
            obj.id = Math.round(message.id);
        }
        if (message.startTime !== 0n) {
            obj.startTime = message.startTime.toString();
        }
        if (message.participants?.length) {
            obj.participants = message.participants.map((e) => Math.round(e));
        }
        if (message.ownerPid !== 0) {
            obj.ownerPid = Math.round(message.ownerPid);
        }
        if (message.hostPid !== 0) {
            obj.hostPid = Math.round(message.hostPid);
        }
        if (message.gameMode !== 0n) {
            obj.gameMode = message.gameMode.toString();
        }
        if (message.flags !== 0n) {
            obj.flags = message.flags.toString();
        }
        if (message.applicationBuffer !== undefined) {
            obj.applicationBuffer = base64FromBytes(message.applicationBuffer);
        }
        return obj;
    },
    create(base) {
        return exports.ActiveMatchesResponse.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseActiveMatchesResponse();
        message.id = object.id ?? 0;
        message.startTime = object.startTime ?? 0n;
        message.participants = object.participants?.map((e) => e) || [];
        message.ownerPid = object.ownerPid ?? 0;
        message.hostPid = object.hostPid ?? 0;
        message.gameMode = object.gameMode ?? 0n;
        message.flags = object.flags ?? 0n;
        message.applicationBuffer = object.applicationBuffer ?? undefined;
        return message;
    },
};
function bytesFromBase64(b64) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}
function base64FromBytes(arr) {
    return globalThis.Buffer.from(arr).toString("base64");
}
function isSet(value) {
    return value !== null && value !== undefined;
}
