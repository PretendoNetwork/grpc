// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: miiverse/v2/get_user_posts_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PostData, PostType, postTypeFromJSON, postTypeToJSON, SortBy, sortByFromJSON, sortByToJSON } from "./post";

export const protobufPackage = "miiverse.v2";

export interface GetUserPostsRequest {
  pid: number;
  allowSpoiler: boolean;
  withMii: boolean;
  searchKey: string[];
  type?: PostType | undefined;
  sortBy?: SortBy | undefined;
  limit?: number | undefined;
  beforeDate?: string | undefined;
  afterDate?: string | undefined;
  offset?: number | undefined;
}

export interface GetUserPostsResponse {
  posts: PostData[];
}

function createBaseGetUserPostsRequest(): GetUserPostsRequest {
  return {
    pid: 0,
    allowSpoiler: false,
    withMii: false,
    searchKey: [],
    type: undefined,
    sortBy: undefined,
    limit: undefined,
    beforeDate: undefined,
    afterDate: undefined,
    offset: undefined,
  };
}

export const GetUserPostsRequest: MessageFns<GetUserPostsRequest> = {
  encode(message: GetUserPostsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    if (message.allowSpoiler !== false) {
      writer.uint32(16).bool(message.allowSpoiler);
    }
    if (message.withMii !== false) {
      writer.uint32(24).bool(message.withMii);
    }
    for (const v of message.searchKey) {
      writer.uint32(34).string(v!);
    }
    if (message.type !== undefined) {
      writer.uint32(40).int32(message.type);
    }
    if (message.sortBy !== undefined) {
      writer.uint32(48).int32(message.sortBy);
    }
    if (message.limit !== undefined) {
      writer.uint32(56).uint32(message.limit);
    }
    if (message.beforeDate !== undefined) {
      writer.uint32(66).string(message.beforeDate);
    }
    if (message.afterDate !== undefined) {
      writer.uint32(74).string(message.afterDate);
    }
    if (message.offset !== undefined) {
      writer.uint32(80).uint32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserPostsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserPostsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.allowSpoiler = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.withMii = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.searchKey.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.limit = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.beforeDate = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.afterDate = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserPostsRequest {
    return {
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      allowSpoiler: isSet(object.allowSpoiler) ? globalThis.Boolean(object.allowSpoiler) : false,
      withMii: isSet(object.withMii) ? globalThis.Boolean(object.withMii) : false,
      searchKey: globalThis.Array.isArray(object?.searchKey)
        ? object.searchKey.map((e: any) => globalThis.String(e))
        : [],
      type: isSet(object.type) ? postTypeFromJSON(object.type) : undefined,
      sortBy: isSet(object.sortBy) ? sortByFromJSON(object.sortBy) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : undefined,
      beforeDate: isSet(object.beforeDate) ? globalThis.String(object.beforeDate) : undefined,
      afterDate: isSet(object.afterDate) ? globalThis.String(object.afterDate) : undefined,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : undefined,
    };
  },

  toJSON(message: GetUserPostsRequest): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.allowSpoiler !== false) {
      obj.allowSpoiler = message.allowSpoiler;
    }
    if (message.withMii !== false) {
      obj.withMii = message.withMii;
    }
    if (message.searchKey?.length) {
      obj.searchKey = message.searchKey;
    }
    if (message.type !== undefined) {
      obj.type = postTypeToJSON(message.type);
    }
    if (message.sortBy !== undefined) {
      obj.sortBy = sortByToJSON(message.sortBy);
    }
    if (message.limit !== undefined) {
      obj.limit = Math.round(message.limit);
    }
    if (message.beforeDate !== undefined) {
      obj.beforeDate = message.beforeDate;
    }
    if (message.afterDate !== undefined) {
      obj.afterDate = message.afterDate;
    }
    if (message.offset !== undefined) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserPostsRequest>): GetUserPostsRequest {
    return GetUserPostsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserPostsRequest>): GetUserPostsRequest {
    const message = createBaseGetUserPostsRequest();
    message.pid = object.pid ?? 0;
    message.allowSpoiler = object.allowSpoiler ?? false;
    message.withMii = object.withMii ?? false;
    message.searchKey = object.searchKey?.map((e) => e) || [];
    message.type = object.type ?? undefined;
    message.sortBy = object.sortBy ?? undefined;
    message.limit = object.limit ?? undefined;
    message.beforeDate = object.beforeDate ?? undefined;
    message.afterDate = object.afterDate ?? undefined;
    message.offset = object.offset ?? undefined;
    return message;
  },
};

function createBaseGetUserPostsResponse(): GetUserPostsResponse {
  return { posts: [] };
}

export const GetUserPostsResponse: MessageFns<GetUserPostsResponse> = {
  encode(message: GetUserPostsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.posts) {
      PostData.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserPostsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserPostsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.posts.push(PostData.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserPostsResponse {
    return { posts: globalThis.Array.isArray(object?.posts) ? object.posts.map((e: any) => PostData.fromJSON(e)) : [] };
  },

  toJSON(message: GetUserPostsResponse): unknown {
    const obj: any = {};
    if (message.posts?.length) {
      obj.posts = message.posts.map((e) => PostData.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserPostsResponse>): GetUserPostsResponse {
    return GetUserPostsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserPostsResponse>): GetUserPostsResponse {
    const message = createBaseGetUserPostsResponse();
    message.posts = object.posts?.map((e) => PostData.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
