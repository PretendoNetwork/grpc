// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: boss/v2/ctr_boss_flags.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "boss.v2";

export interface CTRBOSSFlags {
  markArrivedPrivileged: boolean;
}

function createBaseCTRBOSSFlags(): CTRBOSSFlags {
  return { markArrivedPrivileged: false };
}

export const CTRBOSSFlags: MessageFns<CTRBOSSFlags> = {
  encode(message: CTRBOSSFlags, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.markArrivedPrivileged !== false) {
      writer.uint32(8).bool(message.markArrivedPrivileged);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CTRBOSSFlags {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCTRBOSSFlags();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.markArrivedPrivileged = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CTRBOSSFlags {
    return {
      markArrivedPrivileged: isSet(object.markArrivedPrivileged)
        ? globalThis.Boolean(object.markArrivedPrivileged)
        : false,
    };
  },

  toJSON(message: CTRBOSSFlags): unknown {
    const obj: any = {};
    if (message.markArrivedPrivileged !== false) {
      obj.markArrivedPrivileged = message.markArrivedPrivileged;
    }
    return obj;
  },

  create(base?: DeepPartial<CTRBOSSFlags>): CTRBOSSFlags {
    return CTRBOSSFlags.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CTRBOSSFlags>): CTRBOSSFlags {
    const message = createBaseCTRBOSSFlags();
    message.markArrivedPrivileged = object.markArrivedPrivileged ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
