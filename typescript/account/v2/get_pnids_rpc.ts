// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/get_pnids_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { GetPNIDResponse } from "./get_pnid_rpc";

export const protobufPackage = "account.v2";

export interface GetPNIDsRequest {
  pid: number[];
}

export interface GetPNIDsResponse {
  userData: { [key: number]: GetPNIDResponse };
}

export interface GetPNIDsResponse_UserDataEntry {
  key: number;
  value: GetPNIDResponse | undefined;
}

function createBaseGetPNIDsRequest(): GetPNIDsRequest {
  return { pid: [] };
}

export const GetPNIDsRequest: MessageFns<GetPNIDsRequest> = {
  encode(message: GetPNIDsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.pid) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPNIDsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPNIDsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.pid.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.pid.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPNIDsRequest {
    return { pid: globalThis.Array.isArray(object?.pid) ? object.pid.map((e: any) => globalThis.Number(e)) : [] };
  },

  toJSON(message: GetPNIDsRequest): unknown {
    const obj: any = {};
    if (message.pid?.length) {
      obj.pid = message.pid.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetPNIDsRequest>): GetPNIDsRequest {
    return GetPNIDsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetPNIDsRequest>): GetPNIDsRequest {
    const message = createBaseGetPNIDsRequest();
    message.pid = object.pid?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetPNIDsResponse(): GetPNIDsResponse {
  return { userData: {} };
}

export const GetPNIDsResponse: MessageFns<GetPNIDsResponse> = {
  encode(message: GetPNIDsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    Object.entries(message.userData).forEach(([key, value]) => {
      GetPNIDsResponse_UserDataEntry.encode({ key: key as any, value }, writer.uint32(10).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPNIDsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPNIDsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          const entry1 = GetPNIDsResponse_UserDataEntry.decode(reader, reader.uint32());
          if (entry1.value !== undefined) {
            message.userData[entry1.key] = entry1.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPNIDsResponse {
    return {
      userData: isObject(object.userData)
        ? Object.entries(object.userData).reduce<{ [key: number]: GetPNIDResponse }>((acc, [key, value]) => {
          acc[globalThis.Number(key)] = GetPNIDResponse.fromJSON(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: GetPNIDsResponse): unknown {
    const obj: any = {};
    if (message.userData) {
      const entries = Object.entries(message.userData);
      if (entries.length > 0) {
        obj.userData = {};
        entries.forEach(([k, v]) => {
          obj.userData[k] = GetPNIDResponse.toJSON(v);
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<GetPNIDsResponse>): GetPNIDsResponse {
    return GetPNIDsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetPNIDsResponse>): GetPNIDsResponse {
    const message = createBaseGetPNIDsResponse();
    message.userData = Object.entries(object.userData ?? {}).reduce<{ [key: number]: GetPNIDResponse }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = GetPNIDResponse.fromPartial(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseGetPNIDsResponse_UserDataEntry(): GetPNIDsResponse_UserDataEntry {
  return { key: 0, value: undefined };
}

export const GetPNIDsResponse_UserDataEntry: MessageFns<GetPNIDsResponse_UserDataEntry> = {
  encode(message: GetPNIDsResponse_UserDataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== 0) {
      writer.uint32(8).uint32(message.key);
    }
    if (message.value !== undefined) {
      GetPNIDResponse.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPNIDsResponse_UserDataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPNIDsResponse_UserDataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.key = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = GetPNIDResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPNIDsResponse_UserDataEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? GetPNIDResponse.fromJSON(object.value) : undefined,
    };
  },

  toJSON(message: GetPNIDsResponse_UserDataEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value !== undefined) {
      obj.value = GetPNIDResponse.toJSON(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<GetPNIDsResponse_UserDataEntry>): GetPNIDsResponse_UserDataEntry {
    return GetPNIDsResponse_UserDataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetPNIDsResponse_UserDataEntry>): GetPNIDsResponse_UserDataEntry {
    const message = createBaseGetPNIDsResponse_UserDataEntry();
    message.key = object.key ?? 0;
    message.value = (object.value !== undefined && object.value !== null)
      ? GetPNIDResponse.fromPartial(object.value)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
