// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/list_nex_accounts_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { NEXAccount } from "./nex_account";
import { PageInput, PageResults } from "./page";

export const protobufPackage = "account.v2";

export interface ListNEXAccountsRequest {
  page: PageInput | undefined;
  filter: ListNEXAccountsRequest_Filter | undefined;
}

export interface ListNEXAccountsRequest_Filter {
  pid?: number | undefined;
  friendCode?: number | undefined;
  extraJson?: string | undefined;
}

export interface ListNEXAccountsResponse {
  page: PageResults | undefined;
  items: NEXAccount[];
}

function createBaseListNEXAccountsRequest(): ListNEXAccountsRequest {
  return { page: undefined, filter: undefined };
}

export const ListNEXAccountsRequest: MessageFns<ListNEXAccountsRequest> = {
  encode(message: ListNEXAccountsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageInput.encode(message.page, writer.uint32(10).fork()).join();
    }
    if (message.filter !== undefined) {
      ListNEXAccountsRequest_Filter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListNEXAccountsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListNEXAccountsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageInput.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = ListNEXAccountsRequest_Filter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListNEXAccountsRequest {
    return {
      page: isSet(object.page) ? PageInput.fromJSON(object.page) : undefined,
      filter: isSet(object.filter) ? ListNEXAccountsRequest_Filter.fromJSON(object.filter) : undefined,
    };
  },

  toJSON(message: ListNEXAccountsRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageInput.toJSON(message.page);
    }
    if (message.filter !== undefined) {
      obj.filter = ListNEXAccountsRequest_Filter.toJSON(message.filter);
    }
    return obj;
  },

  create(base?: DeepPartial<ListNEXAccountsRequest>): ListNEXAccountsRequest {
    return ListNEXAccountsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListNEXAccountsRequest>): ListNEXAccountsRequest {
    const message = createBaseListNEXAccountsRequest();
    message.page = (object.page !== undefined && object.page !== null) ? PageInput.fromPartial(object.page) : undefined;
    message.filter = (object.filter !== undefined && object.filter !== null)
      ? ListNEXAccountsRequest_Filter.fromPartial(object.filter)
      : undefined;
    return message;
  },
};

function createBaseListNEXAccountsRequest_Filter(): ListNEXAccountsRequest_Filter {
  return { pid: undefined, friendCode: undefined, extraJson: undefined };
}

export const ListNEXAccountsRequest_Filter: MessageFns<ListNEXAccountsRequest_Filter> = {
  encode(message: ListNEXAccountsRequest_Filter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== undefined) {
      writer.uint32(8).uint32(message.pid);
    }
    if (message.friendCode !== undefined) {
      writer.uint32(16).uint32(message.friendCode);
    }
    if (message.extraJson !== undefined) {
      writer.uint32(26).string(message.extraJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListNEXAccountsRequest_Filter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListNEXAccountsRequest_Filter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.friendCode = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.extraJson = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListNEXAccountsRequest_Filter {
    return {
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : undefined,
      friendCode: isSet(object.friendCode) ? globalThis.Number(object.friendCode) : undefined,
      extraJson: isSet(object.extraJson) ? globalThis.String(object.extraJson) : undefined,
    };
  },

  toJSON(message: ListNEXAccountsRequest_Filter): unknown {
    const obj: any = {};
    if (message.pid !== undefined) {
      obj.pid = Math.round(message.pid);
    }
    if (message.friendCode !== undefined) {
      obj.friendCode = Math.round(message.friendCode);
    }
    if (message.extraJson !== undefined) {
      obj.extraJson = message.extraJson;
    }
    return obj;
  },

  create(base?: DeepPartial<ListNEXAccountsRequest_Filter>): ListNEXAccountsRequest_Filter {
    return ListNEXAccountsRequest_Filter.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListNEXAccountsRequest_Filter>): ListNEXAccountsRequest_Filter {
    const message = createBaseListNEXAccountsRequest_Filter();
    message.pid = object.pid ?? undefined;
    message.friendCode = object.friendCode ?? undefined;
    message.extraJson = object.extraJson ?? undefined;
    return message;
  },
};

function createBaseListNEXAccountsResponse(): ListNEXAccountsResponse {
  return { page: undefined, items: [] };
}

export const ListNEXAccountsResponse: MessageFns<ListNEXAccountsResponse> = {
  encode(message: ListNEXAccountsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageResults.encode(message.page, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      NEXAccount.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListNEXAccountsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListNEXAccountsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageResults.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(NEXAccount.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListNEXAccountsResponse {
    return {
      page: isSet(object.page) ? PageResults.fromJSON(object.page) : undefined,
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => NEXAccount.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListNEXAccountsResponse): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageResults.toJSON(message.page);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => NEXAccount.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListNEXAccountsResponse>): ListNEXAccountsResponse {
    return ListNEXAccountsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListNEXAccountsResponse>): ListNEXAccountsResponse {
    const message = createBaseListNEXAccountsResponse();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResults.fromPartial(object.page)
      : undefined;
    message.items = object.items?.map((e) => NEXAccount.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
