// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/list_bans_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import {
  Ban,
  BanScopeType,
  banScopeTypeFromJSON,
  banScopeTypeToJSON,
  BanTargetType,
  banTargetTypeFromJSON,
  banTargetTypeToJSON,
} from "./ban_details";
import { PageInput, PageResults } from "./page";

export const protobufPackage = "account.v2";

export interface ListBansRequest {
  page: PageInput | undefined;
  filter: ListBansRequest_Filter | undefined;
}

export interface ListBansRequest_Filter {
  targetType: BanTargetType[];
  target: string[];
  issuer: string[];
  startDate?: Date | undefined;
  endDate?: Date | undefined;
  scopeType: BanScopeType[];
  scopeTarget: string[];
  issuedDate?: Date | undefined;
}

export interface ListBansResponse {
  page: PageResults | undefined;
  items: Ban[];
}

function createBaseListBansRequest(): ListBansRequest {
  return { page: undefined, filter: undefined };
}

export const ListBansRequest: MessageFns<ListBansRequest> = {
  encode(message: ListBansRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageInput.encode(message.page, writer.uint32(10).fork()).join();
    }
    if (message.filter !== undefined) {
      ListBansRequest_Filter.encode(message.filter, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBansRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBansRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageInput.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filter = ListBansRequest_Filter.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBansRequest {
    return {
      page: isSet(object.page) ? PageInput.fromJSON(object.page) : undefined,
      filter: isSet(object.filter) ? ListBansRequest_Filter.fromJSON(object.filter) : undefined,
    };
  },

  toJSON(message: ListBansRequest): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageInput.toJSON(message.page);
    }
    if (message.filter !== undefined) {
      obj.filter = ListBansRequest_Filter.toJSON(message.filter);
    }
    return obj;
  },

  create(base?: DeepPartial<ListBansRequest>): ListBansRequest {
    return ListBansRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBansRequest>): ListBansRequest {
    const message = createBaseListBansRequest();
    message.page = (object.page !== undefined && object.page !== null) ? PageInput.fromPartial(object.page) : undefined;
    message.filter = (object.filter !== undefined && object.filter !== null)
      ? ListBansRequest_Filter.fromPartial(object.filter)
      : undefined;
    return message;
  },
};

function createBaseListBansRequest_Filter(): ListBansRequest_Filter {
  return {
    targetType: [],
    target: [],
    issuer: [],
    startDate: undefined,
    endDate: undefined,
    scopeType: [],
    scopeTarget: [],
    issuedDate: undefined,
  };
}

export const ListBansRequest_Filter: MessageFns<ListBansRequest_Filter> = {
  encode(message: ListBansRequest_Filter, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.targetType) {
      writer.int32(v);
    }
    writer.join();
    for (const v of message.target) {
      writer.uint32(18).string(v!);
    }
    for (const v of message.issuer) {
      writer.uint32(26).string(v!);
    }
    if (message.startDate !== undefined) {
      Timestamp.encode(toTimestamp(message.startDate), writer.uint32(34).fork()).join();
    }
    if (message.endDate !== undefined) {
      Timestamp.encode(toTimestamp(message.endDate), writer.uint32(42).fork()).join();
    }
    writer.uint32(50).fork();
    for (const v of message.scopeType) {
      writer.int32(v);
    }
    writer.join();
    for (const v of message.scopeTarget) {
      writer.uint32(58).string(v!);
    }
    if (message.issuedDate !== undefined) {
      Timestamp.encode(toTimestamp(message.issuedDate), writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBansRequest_Filter {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBansRequest_Filter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.targetType.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.targetType.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.issuer.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.scopeType.push(reader.int32() as any);

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.scopeType.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.scopeTarget.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.issuedDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBansRequest_Filter {
    return {
      targetType: globalThis.Array.isArray(object?.targetType)
        ? object.targetType.map((e: any) => banTargetTypeFromJSON(e))
        : [],
      target: globalThis.Array.isArray(object?.target) ? object.target.map((e: any) => globalThis.String(e)) : [],
      issuer: globalThis.Array.isArray(object?.issuer) ? object.issuer.map((e: any) => globalThis.String(e)) : [],
      startDate: isSet(object.startDate) ? fromJsonTimestamp(object.startDate) : undefined,
      endDate: isSet(object.endDate) ? fromJsonTimestamp(object.endDate) : undefined,
      scopeType: globalThis.Array.isArray(object?.scopeType)
        ? object.scopeType.map((e: any) => banScopeTypeFromJSON(e))
        : [],
      scopeTarget: globalThis.Array.isArray(object?.scopeTarget)
        ? object.scopeTarget.map((e: any) => globalThis.String(e))
        : [],
      issuedDate: isSet(object.issuedDate) ? fromJsonTimestamp(object.issuedDate) : undefined,
    };
  },

  toJSON(message: ListBansRequest_Filter): unknown {
    const obj: any = {};
    if (message.targetType?.length) {
      obj.targetType = message.targetType.map((e) => banTargetTypeToJSON(e));
    }
    if (message.target?.length) {
      obj.target = message.target;
    }
    if (message.issuer?.length) {
      obj.issuer = message.issuer;
    }
    if (message.startDate !== undefined) {
      obj.startDate = message.startDate.toISOString();
    }
    if (message.endDate !== undefined) {
      obj.endDate = message.endDate.toISOString();
    }
    if (message.scopeType?.length) {
      obj.scopeType = message.scopeType.map((e) => banScopeTypeToJSON(e));
    }
    if (message.scopeTarget?.length) {
      obj.scopeTarget = message.scopeTarget;
    }
    if (message.issuedDate !== undefined) {
      obj.issuedDate = message.issuedDate.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<ListBansRequest_Filter>): ListBansRequest_Filter {
    return ListBansRequest_Filter.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBansRequest_Filter>): ListBansRequest_Filter {
    const message = createBaseListBansRequest_Filter();
    message.targetType = object.targetType?.map((e) => e) || [];
    message.target = object.target?.map((e) => e) || [];
    message.issuer = object.issuer?.map((e) => e) || [];
    message.startDate = object.startDate ?? undefined;
    message.endDate = object.endDate ?? undefined;
    message.scopeType = object.scopeType?.map((e) => e) || [];
    message.scopeTarget = object.scopeTarget?.map((e) => e) || [];
    message.issuedDate = object.issuedDate ?? undefined;
    return message;
  },
};

function createBaseListBansResponse(): ListBansResponse {
  return { page: undefined, items: [] };
}

export const ListBansResponse: MessageFns<ListBansResponse> = {
  encode(message: ListBansResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.page !== undefined) {
      PageResults.encode(message.page, writer.uint32(10).fork()).join();
    }
    for (const v of message.items) {
      Ban.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListBansResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListBansResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.page = PageResults.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.items.push(Ban.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListBansResponse {
    return {
      page: isSet(object.page) ? PageResults.fromJSON(object.page) : undefined,
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => Ban.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListBansResponse): unknown {
    const obj: any = {};
    if (message.page !== undefined) {
      obj.page = PageResults.toJSON(message.page);
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => Ban.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListBansResponse>): ListBansResponse {
    return ListBansResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListBansResponse>): ListBansResponse {
    const message = createBaseListBansResponse();
    message.page = (object.page !== undefined && object.page !== null)
      ? PageResults.fromPartial(object.page)
      : undefined;
    message.items = object.items?.map((e) => Ban.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
