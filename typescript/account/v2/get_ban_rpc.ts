// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/get_ban_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Ban } from "./ban_details";

export const protobufPackage = "account.v2";

export interface GetBanRequest {
  id: string;
}

export interface GetBanResponse {
  ban: Ban | undefined;
}

function createBaseGetBanRequest(): GetBanRequest {
  return { id: "" };
}

export const GetBanRequest: MessageFns<GetBanRequest> = {
  encode(message: GetBanRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBanRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBanRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBanRequest {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: GetBanRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create(base?: DeepPartial<GetBanRequest>): GetBanRequest {
    return GetBanRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBanRequest>): GetBanRequest {
    const message = createBaseGetBanRequest();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseGetBanResponse(): GetBanResponse {
  return { ban: undefined };
}

export const GetBanResponse: MessageFns<GetBanResponse> = {
  encode(message: GetBanResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ban !== undefined) {
      Ban.encode(message.ban, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetBanResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetBanResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ban = Ban.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetBanResponse {
    return { ban: isSet(object.ban) ? Ban.fromJSON(object.ban) : undefined };
  },

  toJSON(message: GetBanResponse): unknown {
    const obj: any = {};
    if (message.ban !== undefined) {
      obj.ban = Ban.toJSON(message.ban);
    }
    return obj;
  },

  create(base?: DeepPartial<GetBanResponse>): GetBanResponse {
    return GetBanResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetBanResponse>): GetBanResponse {
    const message = createBaseGetBanResponse();
    message.ban = (object.ban !== undefined && object.ban !== null) ? Ban.fromPartial(object.ban) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
