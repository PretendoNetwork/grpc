// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/create_server_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Server } from "./server";

export const protobufPackage = "account.v2";

export interface CreateServerRequest {
  ip: string;
  port: number;
  serviceName: string;
  serviceType: string;
  gameServerId: string;
  titleIds: string[];
  accessMode: string;
  maintenanceMode: boolean;
  device: number;
  aesKey: string;
  clientId?: string | undefined;
}

export interface CreateServerResponse {
  item: Server | undefined;
}

function createBaseCreateServerRequest(): CreateServerRequest {
  return {
    ip: "",
    port: 0,
    serviceName: "",
    serviceType: "",
    gameServerId: "",
    titleIds: [],
    accessMode: "",
    maintenanceMode: false,
    device: 0,
    aesKey: "",
    clientId: undefined,
  };
}

export const CreateServerRequest: MessageFns<CreateServerRequest> = {
  encode(message: CreateServerRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ip !== "") {
      writer.uint32(18).string(message.ip);
    }
    if (message.port !== 0) {
      writer.uint32(24).uint32(message.port);
    }
    if (message.serviceName !== "") {
      writer.uint32(34).string(message.serviceName);
    }
    if (message.serviceType !== "") {
      writer.uint32(42).string(message.serviceType);
    }
    if (message.gameServerId !== "") {
      writer.uint32(50).string(message.gameServerId);
    }
    for (const v of message.titleIds) {
      writer.uint32(58).string(v!);
    }
    if (message.accessMode !== "") {
      writer.uint32(66).string(message.accessMode);
    }
    if (message.maintenanceMode !== false) {
      writer.uint32(72).bool(message.maintenanceMode);
    }
    if (message.device !== 0) {
      writer.uint32(80).uint32(message.device);
    }
    if (message.aesKey !== "") {
      writer.uint32(90).string(message.aesKey);
    }
    if (message.clientId !== undefined) {
      writer.uint32(98).string(message.clientId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateServerRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateServerRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ip = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.port = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.serviceName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.serviceType = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.gameServerId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.titleIds.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.accessMode = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.maintenanceMode = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.device = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.aesKey = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateServerRequest {
    return {
      ip: isSet(object.ip) ? globalThis.String(object.ip) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      serviceName: isSet(object.serviceName) ? globalThis.String(object.serviceName) : "",
      serviceType: isSet(object.serviceType) ? globalThis.String(object.serviceType) : "",
      gameServerId: isSet(object.gameServerId) ? globalThis.String(object.gameServerId) : "",
      titleIds: globalThis.Array.isArray(object?.titleIds) ? object.titleIds.map((e: any) => globalThis.String(e)) : [],
      accessMode: isSet(object.accessMode) ? globalThis.String(object.accessMode) : "",
      maintenanceMode: isSet(object.maintenanceMode) ? globalThis.Boolean(object.maintenanceMode) : false,
      device: isSet(object.device) ? globalThis.Number(object.device) : 0,
      aesKey: isSet(object.aesKey) ? globalThis.String(object.aesKey) : "",
      clientId: isSet(object.clientId) ? globalThis.String(object.clientId) : undefined,
    };
  },

  toJSON(message: CreateServerRequest): unknown {
    const obj: any = {};
    if (message.ip !== "") {
      obj.ip = message.ip;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.serviceName !== "") {
      obj.serviceName = message.serviceName;
    }
    if (message.serviceType !== "") {
      obj.serviceType = message.serviceType;
    }
    if (message.gameServerId !== "") {
      obj.gameServerId = message.gameServerId;
    }
    if (message.titleIds?.length) {
      obj.titleIds = message.titleIds;
    }
    if (message.accessMode !== "") {
      obj.accessMode = message.accessMode;
    }
    if (message.maintenanceMode !== false) {
      obj.maintenanceMode = message.maintenanceMode;
    }
    if (message.device !== 0) {
      obj.device = Math.round(message.device);
    }
    if (message.aesKey !== "") {
      obj.aesKey = message.aesKey;
    }
    if (message.clientId !== undefined) {
      obj.clientId = message.clientId;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateServerRequest>): CreateServerRequest {
    return CreateServerRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateServerRequest>): CreateServerRequest {
    const message = createBaseCreateServerRequest();
    message.ip = object.ip ?? "";
    message.port = object.port ?? 0;
    message.serviceName = object.serviceName ?? "";
    message.serviceType = object.serviceType ?? "";
    message.gameServerId = object.gameServerId ?? "";
    message.titleIds = object.titleIds?.map((e) => e) || [];
    message.accessMode = object.accessMode ?? "";
    message.maintenanceMode = object.maintenanceMode ?? false;
    message.device = object.device ?? 0;
    message.aesKey = object.aesKey ?? "";
    message.clientId = object.clientId ?? undefined;
    return message;
  },
};

function createBaseCreateServerResponse(): CreateServerResponse {
  return { item: undefined };
}

export const CreateServerResponse: MessageFns<CreateServerResponse> = {
  encode(message: CreateServerResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.item !== undefined) {
      Server.encode(message.item, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateServerResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateServerResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.item = Server.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateServerResponse {
    return { item: isSet(object.item) ? Server.fromJSON(object.item) : undefined };
  },

  toJSON(message: CreateServerResponse): unknown {
    const obj: any = {};
    if (message.item !== undefined) {
      obj.item = Server.toJSON(message.item);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateServerResponse>): CreateServerResponse {
    return CreateServerResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateServerResponse>): CreateServerResponse {
    const message = createBaseCreateServerResponse();
    message.item = (object.item !== undefined && object.item !== null) ? Server.fromPartial(object.item) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
