// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/update_nex_account_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { NEXAccount } from "./nex_account";

export const protobufPackage = "account.v2";

export interface UpdateNEXAccountRequest {
  pid: number;
  serverAccessLevel?: string | undefined;
  accessLevel?: number | undefined;
}

export interface UpdateNEXAccountResponse {
  nexAccount: NEXAccount | undefined;
}

function createBaseUpdateNEXAccountRequest(): UpdateNEXAccountRequest {
  return { pid: 0, serverAccessLevel: undefined, accessLevel: undefined };
}

export const UpdateNEXAccountRequest: MessageFns<UpdateNEXAccountRequest> = {
  encode(message: UpdateNEXAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    if (message.serverAccessLevel !== undefined) {
      writer.uint32(18).string(message.serverAccessLevel);
    }
    if (message.accessLevel !== undefined) {
      writer.uint32(24).int32(message.accessLevel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateNEXAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateNEXAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serverAccessLevel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.accessLevel = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateNEXAccountRequest {
    return {
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      serverAccessLevel: isSet(object.serverAccessLevel) ? globalThis.String(object.serverAccessLevel) : undefined,
      accessLevel: isSet(object.accessLevel) ? globalThis.Number(object.accessLevel) : undefined,
    };
  },

  toJSON(message: UpdateNEXAccountRequest): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.serverAccessLevel !== undefined) {
      obj.serverAccessLevel = message.serverAccessLevel;
    }
    if (message.accessLevel !== undefined) {
      obj.accessLevel = Math.round(message.accessLevel);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateNEXAccountRequest>): UpdateNEXAccountRequest {
    return UpdateNEXAccountRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateNEXAccountRequest>): UpdateNEXAccountRequest {
    const message = createBaseUpdateNEXAccountRequest();
    message.pid = object.pid ?? 0;
    message.serverAccessLevel = object.serverAccessLevel ?? undefined;
    message.accessLevel = object.accessLevel ?? undefined;
    return message;
  },
};

function createBaseUpdateNEXAccountResponse(): UpdateNEXAccountResponse {
  return { nexAccount: undefined };
}

export const UpdateNEXAccountResponse: MessageFns<UpdateNEXAccountResponse> = {
  encode(message: UpdateNEXAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nexAccount !== undefined) {
      NEXAccount.encode(message.nexAccount, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateNEXAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateNEXAccountResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nexAccount = NEXAccount.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateNEXAccountResponse {
    return { nexAccount: isSet(object.nexAccount) ? NEXAccount.fromJSON(object.nexAccount) : undefined };
  },

  toJSON(message: UpdateNEXAccountResponse): unknown {
    const obj: any = {};
    if (message.nexAccount !== undefined) {
      obj.nexAccount = NEXAccount.toJSON(message.nexAccount);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateNEXAccountResponse>): UpdateNEXAccountResponse {
    return UpdateNEXAccountResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateNEXAccountResponse>): UpdateNEXAccountResponse {
    const message = createBaseUpdateNEXAccountResponse();
    message.nexAccount = (object.nexAccount !== undefined && object.nexAccount !== null)
      ? NEXAccount.fromPartial(object.nexAccount)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
