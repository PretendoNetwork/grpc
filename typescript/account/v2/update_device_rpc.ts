// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: account/v2/update_device_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Device } from "./device";

export const protobufPackage = "account.v2";

export interface UpdateDeviceRequest {
  deviceId: number;
  serverAccessLevel?: string | undefined;
  accessLevel?: number | undefined;
}

export interface UpdateDeviceResponse {
  device: Device | undefined;
}

function createBaseUpdateDeviceRequest(): UpdateDeviceRequest {
  return { deviceId: 0, serverAccessLevel: undefined, accessLevel: undefined };
}

export const UpdateDeviceRequest: MessageFns<UpdateDeviceRequest> = {
  encode(message: UpdateDeviceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== 0) {
      writer.uint32(8).uint32(message.deviceId);
    }
    if (message.serverAccessLevel !== undefined) {
      writer.uint32(18).string(message.serverAccessLevel);
    }
    if (message.accessLevel !== undefined) {
      writer.uint32(24).int32(message.accessLevel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateDeviceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateDeviceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deviceId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serverAccessLevel = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.accessLevel = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateDeviceRequest {
    return {
      deviceId: isSet(object.deviceId) ? globalThis.Number(object.deviceId) : 0,
      serverAccessLevel: isSet(object.serverAccessLevel) ? globalThis.String(object.serverAccessLevel) : undefined,
      accessLevel: isSet(object.accessLevel) ? globalThis.Number(object.accessLevel) : undefined,
    };
  },

  toJSON(message: UpdateDeviceRequest): unknown {
    const obj: any = {};
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    if (message.serverAccessLevel !== undefined) {
      obj.serverAccessLevel = message.serverAccessLevel;
    }
    if (message.accessLevel !== undefined) {
      obj.accessLevel = Math.round(message.accessLevel);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateDeviceRequest>): UpdateDeviceRequest {
    return UpdateDeviceRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateDeviceRequest>): UpdateDeviceRequest {
    const message = createBaseUpdateDeviceRequest();
    message.deviceId = object.deviceId ?? 0;
    message.serverAccessLevel = object.serverAccessLevel ?? undefined;
    message.accessLevel = object.accessLevel ?? undefined;
    return message;
  },
};

function createBaseUpdateDeviceResponse(): UpdateDeviceResponse {
  return { device: undefined };
}

export const UpdateDeviceResponse: MessageFns<UpdateDeviceResponse> = {
  encode(message: UpdateDeviceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.device !== undefined) {
      Device.encode(message.device, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateDeviceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateDeviceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.device = Device.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateDeviceResponse {
    return { device: isSet(object.device) ? Device.fromJSON(object.device) : undefined };
  },

  toJSON(message: UpdateDeviceResponse): unknown {
    const obj: any = {};
    if (message.device !== undefined) {
      obj.device = Device.toJSON(message.device);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateDeviceResponse>): UpdateDeviceResponse {
    return UpdateDeviceResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateDeviceResponse>): UpdateDeviceResponse {
    const message = createBaseUpdateDeviceResponse();
    message.device = (object.device !== undefined && object.device !== null)
      ? Device.fromPartial(object.device)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
