// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: friends/deny_friend_request_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "friends";

export interface DenyFriendRequestRequest {
  friendRequestId: bigint;
}

export interface DenyFriendRequestResponse {
  success: boolean;
}

function createBaseDenyFriendRequestRequest(): DenyFriendRequestRequest {
  return { friendRequestId: 0n };
}

export const DenyFriendRequestRequest: MessageFns<DenyFriendRequestRequest> = {
  encode(message: DenyFriendRequestRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.friendRequestId !== 0n) {
      if (BigInt.asUintN(64, message.friendRequestId) !== message.friendRequestId) {
        throw new globalThis.Error("value provided for field message.friendRequestId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.friendRequestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DenyFriendRequestRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDenyFriendRequestRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.friendRequestId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DenyFriendRequestRequest {
    return { friendRequestId: isSet(object.friendRequestId) ? BigInt(object.friendRequestId) : 0n };
  },

  toJSON(message: DenyFriendRequestRequest): unknown {
    const obj: any = {};
    if (message.friendRequestId !== 0n) {
      obj.friendRequestId = message.friendRequestId.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<DenyFriendRequestRequest>): DenyFriendRequestRequest {
    return DenyFriendRequestRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DenyFriendRequestRequest>): DenyFriendRequestRequest {
    const message = createBaseDenyFriendRequestRequest();
    message.friendRequestId = object.friendRequestId ?? 0n;
    return message;
  },
};

function createBaseDenyFriendRequestResponse(): DenyFriendRequestResponse {
  return { success: false };
}

export const DenyFriendRequestResponse: MessageFns<DenyFriendRequestResponse> = {
  encode(message: DenyFriendRequestResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DenyFriendRequestResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDenyFriendRequestResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DenyFriendRequestResponse {
    return { success: isSet(object.success) ? globalThis.Boolean(object.success) : false };
  },

  toJSON(message: DenyFriendRequestResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    return obj;
  },

  create(base?: DeepPartial<DenyFriendRequestResponse>): DenyFriendRequestResponse {
    return DenyFriendRequestResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DenyFriendRequestResponse>): DenyFriendRequestResponse {
    const message = createBaseDenyFriendRequestResponse();
    message.success = object.success ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
