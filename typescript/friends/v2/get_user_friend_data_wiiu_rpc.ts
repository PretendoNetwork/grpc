// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: friends/v2/get_user_friend_data_wiiu_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FriendInfoWiiU } from "./friend_info";

export const protobufPackage = "friends.v2";

export interface GetUserFriendsDataWiiURequest {
  pid: number;
}

export interface GetUserFriendsDataWiiUResponse {
  friends: FriendInfoWiiU[];
}

function createBaseGetUserFriendsDataWiiURequest(): GetUserFriendsDataWiiURequest {
  return { pid: 0 };
}

export const GetUserFriendsDataWiiURequest: MessageFns<GetUserFriendsDataWiiURequest> = {
  encode(message: GetUserFriendsDataWiiURequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserFriendsDataWiiURequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserFriendsDataWiiURequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserFriendsDataWiiURequest {
    return { pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0 };
  },

  toJSON(message: GetUserFriendsDataWiiURequest): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserFriendsDataWiiURequest>): GetUserFriendsDataWiiURequest {
    return GetUserFriendsDataWiiURequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserFriendsDataWiiURequest>): GetUserFriendsDataWiiURequest {
    const message = createBaseGetUserFriendsDataWiiURequest();
    message.pid = object.pid ?? 0;
    return message;
  },
};

function createBaseGetUserFriendsDataWiiUResponse(): GetUserFriendsDataWiiUResponse {
  return { friends: [] };
}

export const GetUserFriendsDataWiiUResponse: MessageFns<GetUserFriendsDataWiiUResponse> = {
  encode(message: GetUserFriendsDataWiiUResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.friends) {
      FriendInfoWiiU.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserFriendsDataWiiUResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserFriendsDataWiiUResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.friends.push(FriendInfoWiiU.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserFriendsDataWiiUResponse {
    return {
      friends: globalThis.Array.isArray(object?.friends)
        ? object.friends.map((e: any) => FriendInfoWiiU.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetUserFriendsDataWiiUResponse): unknown {
    const obj: any = {};
    if (message.friends?.length) {
      obj.friends = message.friends.map((e) => FriendInfoWiiU.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserFriendsDataWiiUResponse>): GetUserFriendsDataWiiUResponse {
    return GetUserFriendsDataWiiUResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserFriendsDataWiiUResponse>): GetUserFriendsDataWiiUResponse {
    const message = createBaseGetUserFriendsDataWiiUResponse();
    message.friends = object.friends?.map((e) => FriendInfoWiiU.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
