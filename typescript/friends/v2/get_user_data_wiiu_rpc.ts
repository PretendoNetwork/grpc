// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: friends/v2/get_user_data_wiiu_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FriendInfoWiiU } from "./friend_info";

export const protobufPackage = "friends.v2";

export interface GetUserDataWiiURequest {
  pid: number;
}

export interface GetUserDataWiiUResponse {
  user: FriendInfoWiiU | undefined;
}

function createBaseGetUserDataWiiURequest(): GetUserDataWiiURequest {
  return { pid: 0 };
}

export const GetUserDataWiiURequest: MessageFns<GetUserDataWiiURequest> = {
  encode(message: GetUserDataWiiURequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserDataWiiURequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserDataWiiURequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserDataWiiURequest {
    return { pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0 };
  },

  toJSON(message: GetUserDataWiiURequest): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserDataWiiURequest>): GetUserDataWiiURequest {
    return GetUserDataWiiURequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserDataWiiURequest>): GetUserDataWiiURequest {
    const message = createBaseGetUserDataWiiURequest();
    message.pid = object.pid ?? 0;
    return message;
  },
};

function createBaseGetUserDataWiiUResponse(): GetUserDataWiiUResponse {
  return { user: undefined };
}

export const GetUserDataWiiUResponse: MessageFns<GetUserDataWiiUResponse> = {
  encode(message: GetUserDataWiiUResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      FriendInfoWiiU.encode(message.user, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserDataWiiUResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserDataWiiUResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = FriendInfoWiiU.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserDataWiiUResponse {
    return { user: isSet(object.user) ? FriendInfoWiiU.fromJSON(object.user) : undefined };
  },

  toJSON(message: GetUserDataWiiUResponse): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = FriendInfoWiiU.toJSON(message.user);
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserDataWiiUResponse>): GetUserDataWiiUResponse {
    return GetUserDataWiiUResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserDataWiiUResponse>): GetUserDataWiiUResponse {
    const message = createBaseGetUserDataWiiUResponse();
    message.user = (object.user !== undefined && object.user !== null)
      ? FriendInfoWiiU.fromPartial(object.user)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
