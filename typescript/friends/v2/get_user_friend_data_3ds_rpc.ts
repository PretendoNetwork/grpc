// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: friends/v2/get_user_friend_data_3ds_rpc.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FriendInfo3DS } from "./friend_info";

export const protobufPackage = "friends.v2";

export interface GetUserFriendsData3DSRequest {
  pid: number;
}

export interface GetUserFriendsData3DSResponse {
  friends: FriendInfo3DS[];
}

function createBaseGetUserFriendsData3DSRequest(): GetUserFriendsData3DSRequest {
  return { pid: 0 };
}

export const GetUserFriendsData3DSRequest: MessageFns<GetUserFriendsData3DSRequest> = {
  encode(message: GetUserFriendsData3DSRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserFriendsData3DSRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserFriendsData3DSRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserFriendsData3DSRequest {
    return { pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0 };
  },

  toJSON(message: GetUserFriendsData3DSRequest): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserFriendsData3DSRequest>): GetUserFriendsData3DSRequest {
    return GetUserFriendsData3DSRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserFriendsData3DSRequest>): GetUserFriendsData3DSRequest {
    const message = createBaseGetUserFriendsData3DSRequest();
    message.pid = object.pid ?? 0;
    return message;
  },
};

function createBaseGetUserFriendsData3DSResponse(): GetUserFriendsData3DSResponse {
  return { friends: [] };
}

export const GetUserFriendsData3DSResponse: MessageFns<GetUserFriendsData3DSResponse> = {
  encode(message: GetUserFriendsData3DSResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.friends) {
      FriendInfo3DS.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserFriendsData3DSResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserFriendsData3DSResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.friends.push(FriendInfo3DS.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserFriendsData3DSResponse {
    return {
      friends: globalThis.Array.isArray(object?.friends)
        ? object.friends.map((e: any) => FriendInfo3DS.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetUserFriendsData3DSResponse): unknown {
    const obj: any = {};
    if (message.friends?.length) {
      obj.friends = message.friends.map((e) => FriendInfo3DS.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserFriendsData3DSResponse>): GetUserFriendsData3DSResponse {
    return GetUserFriendsData3DSResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserFriendsData3DSResponse>): GetUserFriendsData3DSResponse {
    const message = createBaseGetUserFriendsData3DSResponse();
    message.friends = object.friends?.map((e) => FriendInfo3DS.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
