// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: friends/v2/friend_info.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "friends.v2";

export interface Comment {
  contents: string;
  lastChanged: Date | undefined;
}

/** 3DS Mii */
export interface Mii {
  name: string;
  profanityFlag: boolean;
  characterSet: number;
  miiData: Buffer;
  miiDataEncrypted: Buffer;
}

export interface FriendMii {
  pid: number;
  mii: Mii | undefined;
  modifiedAt: Date | undefined;
}

/** Wii U Mii */
export interface MiiV2 {
  name: string;
  miiData: Buffer;
  datetime: Date | undefined;
}

export interface PrincipalBasicInfo {
  pid: number;
  nnid: string;
  mii: MiiV2 | undefined;
}

export interface NNAInfo {
  principalBasicInfo: PrincipalBasicInfo | undefined;
}

export interface GameKey {
  titleId: bigint;
  titleVersion: number;
}

/** Wii U Presence */
export interface NintendoPresenceV2 {
  changedFlags: number;
  online: boolean;
  gameKey: GameKey | undefined;
  message: string;
  gameServerId: number;
  pid: number;
  gatheringId: number;
  applicationData: Buffer;
}

/** 3DS Presence */
export interface NintendoPresence {
  changedFlags: number;
  gameKey: GameKey | undefined;
  message: string;
  joinAvailableFlag: number;
  matchmakeType: number;
  joinGameId: number;
  joinGameMode: number;
  ownerPid: number;
  joinGroupId: number;
  applicationArg: Buffer;
}

export interface FriendInfoWiiU {
  nnaInfo: NNAInfo | undefined;
  presence: NintendoPresenceV2 | undefined;
  status: Comment | undefined;
  becameFriend: Date | undefined;
  lastOnline: Date | undefined;
}

export interface FriendInfo3DS {
  pid: number;
  region: number;
  country: number;
  area: number;
  language: number;
  platform: number;
  presence: NintendoPresence | undefined;
  gameKey: GameKey | undefined;
  message: string;
  messageUpdatedAt: Date | undefined;
  miiModifiedAt: Date | undefined;
  lastOnline: Date | undefined;
  mii: FriendMii | undefined;
}

function createBaseComment(): Comment {
  return { contents: "", lastChanged: undefined };
}

export const Comment: MessageFns<Comment> = {
  encode(message: Comment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.contents !== "") {
      writer.uint32(18).string(message.contents);
    }
    if (message.lastChanged !== undefined) {
      Timestamp.encode(toTimestamp(message.lastChanged), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Comment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contents = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.lastChanged = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Comment {
    return {
      contents: isSet(object.contents) ? globalThis.String(object.contents) : "",
      lastChanged: isSet(object.lastChanged) ? fromJsonTimestamp(object.lastChanged) : undefined,
    };
  },

  toJSON(message: Comment): unknown {
    const obj: any = {};
    if (message.contents !== "") {
      obj.contents = message.contents;
    }
    if (message.lastChanged !== undefined) {
      obj.lastChanged = message.lastChanged.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Comment>): Comment {
    return Comment.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Comment>): Comment {
    const message = createBaseComment();
    message.contents = object.contents ?? "";
    message.lastChanged = object.lastChanged ?? undefined;
    return message;
  },
};

function createBaseMii(): Mii {
  return {
    name: "",
    profanityFlag: false,
    characterSet: 0,
    miiData: Buffer.alloc(0),
    miiDataEncrypted: Buffer.alloc(0),
  };
}

export const Mii: MessageFns<Mii> = {
  encode(message: Mii, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.profanityFlag !== false) {
      writer.uint32(16).bool(message.profanityFlag);
    }
    if (message.characterSet !== 0) {
      writer.uint32(24).uint32(message.characterSet);
    }
    if (message.miiData.length !== 0) {
      writer.uint32(34).bytes(message.miiData);
    }
    if (message.miiDataEncrypted.length !== 0) {
      writer.uint32(42).bytes(message.miiDataEncrypted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Mii {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMii();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.profanityFlag = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.characterSet = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.miiData = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.miiDataEncrypted = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Mii {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      profanityFlag: isSet(object.profanityFlag) ? globalThis.Boolean(object.profanityFlag) : false,
      characterSet: isSet(object.characterSet) ? globalThis.Number(object.characterSet) : 0,
      miiData: isSet(object.miiData) ? Buffer.from(bytesFromBase64(object.miiData)) : Buffer.alloc(0),
      miiDataEncrypted: isSet(object.miiDataEncrypted)
        ? Buffer.from(bytesFromBase64(object.miiDataEncrypted))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: Mii): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.profanityFlag !== false) {
      obj.profanityFlag = message.profanityFlag;
    }
    if (message.characterSet !== 0) {
      obj.characterSet = Math.round(message.characterSet);
    }
    if (message.miiData.length !== 0) {
      obj.miiData = base64FromBytes(message.miiData);
    }
    if (message.miiDataEncrypted.length !== 0) {
      obj.miiDataEncrypted = base64FromBytes(message.miiDataEncrypted);
    }
    return obj;
  },

  create(base?: DeepPartial<Mii>): Mii {
    return Mii.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Mii>): Mii {
    const message = createBaseMii();
    message.name = object.name ?? "";
    message.profanityFlag = object.profanityFlag ?? false;
    message.characterSet = object.characterSet ?? 0;
    message.miiData = object.miiData ?? Buffer.alloc(0);
    message.miiDataEncrypted = object.miiDataEncrypted ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseFriendMii(): FriendMii {
  return { pid: 0, mii: undefined, modifiedAt: undefined };
}

export const FriendMii: MessageFns<FriendMii> = {
  encode(message: FriendMii, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    if (message.mii !== undefined) {
      Mii.encode(message.mii, writer.uint32(18).fork()).join();
    }
    if (message.modifiedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.modifiedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendMii {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendMii();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mii = Mii.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.modifiedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendMii {
    return {
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      mii: isSet(object.mii) ? Mii.fromJSON(object.mii) : undefined,
      modifiedAt: isSet(object.modifiedAt) ? fromJsonTimestamp(object.modifiedAt) : undefined,
    };
  },

  toJSON(message: FriendMii): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.mii !== undefined) {
      obj.mii = Mii.toJSON(message.mii);
    }
    if (message.modifiedAt !== undefined) {
      obj.modifiedAt = message.modifiedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<FriendMii>): FriendMii {
    return FriendMii.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FriendMii>): FriendMii {
    const message = createBaseFriendMii();
    message.pid = object.pid ?? 0;
    message.mii = (object.mii !== undefined && object.mii !== null) ? Mii.fromPartial(object.mii) : undefined;
    message.modifiedAt = object.modifiedAt ?? undefined;
    return message;
  },
};

function createBaseMiiV2(): MiiV2 {
  return { name: "", miiData: Buffer.alloc(0), datetime: undefined };
}

export const MiiV2: MessageFns<MiiV2> = {
  encode(message: MiiV2, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.miiData.length !== 0) {
      writer.uint32(34).bytes(message.miiData);
    }
    if (message.datetime !== undefined) {
      Timestamp.encode(toTimestamp(message.datetime), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MiiV2 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMiiV2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.miiData = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.datetime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MiiV2 {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      miiData: isSet(object.miiData) ? Buffer.from(bytesFromBase64(object.miiData)) : Buffer.alloc(0),
      datetime: isSet(object.datetime) ? fromJsonTimestamp(object.datetime) : undefined,
    };
  },

  toJSON(message: MiiV2): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.miiData.length !== 0) {
      obj.miiData = base64FromBytes(message.miiData);
    }
    if (message.datetime !== undefined) {
      obj.datetime = message.datetime.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<MiiV2>): MiiV2 {
    return MiiV2.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MiiV2>): MiiV2 {
    const message = createBaseMiiV2();
    message.name = object.name ?? "";
    message.miiData = object.miiData ?? Buffer.alloc(0);
    message.datetime = object.datetime ?? undefined;
    return message;
  },
};

function createBasePrincipalBasicInfo(): PrincipalBasicInfo {
  return { pid: 0, nnid: "", mii: undefined };
}

export const PrincipalBasicInfo: MessageFns<PrincipalBasicInfo> = {
  encode(message: PrincipalBasicInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    if (message.nnid !== "") {
      writer.uint32(18).string(message.nnid);
    }
    if (message.mii !== undefined) {
      MiiV2.encode(message.mii, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrincipalBasicInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrincipalBasicInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nnid = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mii = MiiV2.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrincipalBasicInfo {
    return {
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      nnid: isSet(object.nnid) ? globalThis.String(object.nnid) : "",
      mii: isSet(object.mii) ? MiiV2.fromJSON(object.mii) : undefined,
    };
  },

  toJSON(message: PrincipalBasicInfo): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.nnid !== "") {
      obj.nnid = message.nnid;
    }
    if (message.mii !== undefined) {
      obj.mii = MiiV2.toJSON(message.mii);
    }
    return obj;
  },

  create(base?: DeepPartial<PrincipalBasicInfo>): PrincipalBasicInfo {
    return PrincipalBasicInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PrincipalBasicInfo>): PrincipalBasicInfo {
    const message = createBasePrincipalBasicInfo();
    message.pid = object.pid ?? 0;
    message.nnid = object.nnid ?? "";
    message.mii = (object.mii !== undefined && object.mii !== null) ? MiiV2.fromPartial(object.mii) : undefined;
    return message;
  },
};

function createBaseNNAInfo(): NNAInfo {
  return { principalBasicInfo: undefined };
}

export const NNAInfo: MessageFns<NNAInfo> = {
  encode(message: NNAInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.principalBasicInfo !== undefined) {
      PrincipalBasicInfo.encode(message.principalBasicInfo, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NNAInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNNAInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.principalBasicInfo = PrincipalBasicInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NNAInfo {
    return {
      principalBasicInfo: isSet(object.principalBasicInfo)
        ? PrincipalBasicInfo.fromJSON(object.principalBasicInfo)
        : undefined,
    };
  },

  toJSON(message: NNAInfo): unknown {
    const obj: any = {};
    if (message.principalBasicInfo !== undefined) {
      obj.principalBasicInfo = PrincipalBasicInfo.toJSON(message.principalBasicInfo);
    }
    return obj;
  },

  create(base?: DeepPartial<NNAInfo>): NNAInfo {
    return NNAInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NNAInfo>): NNAInfo {
    const message = createBaseNNAInfo();
    message.principalBasicInfo = (object.principalBasicInfo !== undefined && object.principalBasicInfo !== null)
      ? PrincipalBasicInfo.fromPartial(object.principalBasicInfo)
      : undefined;
    return message;
  },
};

function createBaseGameKey(): GameKey {
  return { titleId: 0n, titleVersion: 0 };
}

export const GameKey: MessageFns<GameKey> = {
  encode(message: GameKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.titleId !== 0n) {
      if (BigInt.asUintN(64, message.titleId) !== message.titleId) {
        throw new globalThis.Error("value provided for field message.titleId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.titleId);
    }
    if (message.titleVersion !== 0) {
      writer.uint32(16).uint32(message.titleVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.titleId = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.titleVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameKey {
    return {
      titleId: isSet(object.titleId) ? BigInt(object.titleId) : 0n,
      titleVersion: isSet(object.titleVersion) ? globalThis.Number(object.titleVersion) : 0,
    };
  },

  toJSON(message: GameKey): unknown {
    const obj: any = {};
    if (message.titleId !== 0n) {
      obj.titleId = message.titleId.toString();
    }
    if (message.titleVersion !== 0) {
      obj.titleVersion = Math.round(message.titleVersion);
    }
    return obj;
  },

  create(base?: DeepPartial<GameKey>): GameKey {
    return GameKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GameKey>): GameKey {
    const message = createBaseGameKey();
    message.titleId = object.titleId ?? 0n;
    message.titleVersion = object.titleVersion ?? 0;
    return message;
  },
};

function createBaseNintendoPresenceV2(): NintendoPresenceV2 {
  return {
    changedFlags: 0,
    online: false,
    gameKey: undefined,
    message: "",
    gameServerId: 0,
    pid: 0,
    gatheringId: 0,
    applicationData: Buffer.alloc(0),
  };
}

export const NintendoPresenceV2: MessageFns<NintendoPresenceV2> = {
  encode(message: NintendoPresenceV2, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.changedFlags !== 0) {
      writer.uint32(8).uint32(message.changedFlags);
    }
    if (message.online !== false) {
      writer.uint32(16).bool(message.online);
    }
    if (message.gameKey !== undefined) {
      GameKey.encode(message.gameKey, writer.uint32(26).fork()).join();
    }
    if (message.message !== "") {
      writer.uint32(42).string(message.message);
    }
    if (message.gameServerId !== 0) {
      writer.uint32(64).uint32(message.gameServerId);
    }
    if (message.pid !== 0) {
      writer.uint32(80).uint32(message.pid);
    }
    if (message.gatheringId !== 0) {
      writer.uint32(88).uint32(message.gatheringId);
    }
    if (message.applicationData.length !== 0) {
      writer.uint32(98).bytes(message.applicationData);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NintendoPresenceV2 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNintendoPresenceV2();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.changedFlags = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.online = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gameKey = GameKey.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.gameServerId = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.gatheringId = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.applicationData = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NintendoPresenceV2 {
    return {
      changedFlags: isSet(object.changedFlags) ? globalThis.Number(object.changedFlags) : 0,
      online: isSet(object.online) ? globalThis.Boolean(object.online) : false,
      gameKey: isSet(object.gameKey) ? GameKey.fromJSON(object.gameKey) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      gameServerId: isSet(object.gameServerId) ? globalThis.Number(object.gameServerId) : 0,
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      gatheringId: isSet(object.gatheringId) ? globalThis.Number(object.gatheringId) : 0,
      applicationData: isSet(object.applicationData)
        ? Buffer.from(bytesFromBase64(object.applicationData))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: NintendoPresenceV2): unknown {
    const obj: any = {};
    if (message.changedFlags !== 0) {
      obj.changedFlags = Math.round(message.changedFlags);
    }
    if (message.online !== false) {
      obj.online = message.online;
    }
    if (message.gameKey !== undefined) {
      obj.gameKey = GameKey.toJSON(message.gameKey);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.gameServerId !== 0) {
      obj.gameServerId = Math.round(message.gameServerId);
    }
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.gatheringId !== 0) {
      obj.gatheringId = Math.round(message.gatheringId);
    }
    if (message.applicationData.length !== 0) {
      obj.applicationData = base64FromBytes(message.applicationData);
    }
    return obj;
  },

  create(base?: DeepPartial<NintendoPresenceV2>): NintendoPresenceV2 {
    return NintendoPresenceV2.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NintendoPresenceV2>): NintendoPresenceV2 {
    const message = createBaseNintendoPresenceV2();
    message.changedFlags = object.changedFlags ?? 0;
    message.online = object.online ?? false;
    message.gameKey = (object.gameKey !== undefined && object.gameKey !== null)
      ? GameKey.fromPartial(object.gameKey)
      : undefined;
    message.message = object.message ?? "";
    message.gameServerId = object.gameServerId ?? 0;
    message.pid = object.pid ?? 0;
    message.gatheringId = object.gatheringId ?? 0;
    message.applicationData = object.applicationData ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseNintendoPresence(): NintendoPresence {
  return {
    changedFlags: 0,
    gameKey: undefined,
    message: "",
    joinAvailableFlag: 0,
    matchmakeType: 0,
    joinGameId: 0,
    joinGameMode: 0,
    ownerPid: 0,
    joinGroupId: 0,
    applicationArg: Buffer.alloc(0),
  };
}

export const NintendoPresence: MessageFns<NintendoPresence> = {
  encode(message: NintendoPresence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.changedFlags !== 0) {
      writer.uint32(8).uint32(message.changedFlags);
    }
    if (message.gameKey !== undefined) {
      GameKey.encode(message.gameKey, writer.uint32(18).fork()).join();
    }
    if (message.message !== "") {
      writer.uint32(26).string(message.message);
    }
    if (message.joinAvailableFlag !== 0) {
      writer.uint32(32).uint32(message.joinAvailableFlag);
    }
    if (message.matchmakeType !== 0) {
      writer.uint32(40).uint32(message.matchmakeType);
    }
    if (message.joinGameId !== 0) {
      writer.uint32(48).uint32(message.joinGameId);
    }
    if (message.joinGameMode !== 0) {
      writer.uint32(56).uint32(message.joinGameMode);
    }
    if (message.ownerPid !== 0) {
      writer.uint32(64).uint32(message.ownerPid);
    }
    if (message.joinGroupId !== 0) {
      writer.uint32(72).uint32(message.joinGroupId);
    }
    if (message.applicationArg.length !== 0) {
      writer.uint32(82).bytes(message.applicationArg);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NintendoPresence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNintendoPresence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.changedFlags = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.gameKey = GameKey.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.joinAvailableFlag = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.matchmakeType = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.joinGameId = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.joinGameMode = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.ownerPid = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.joinGroupId = reader.uint32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.applicationArg = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NintendoPresence {
    return {
      changedFlags: isSet(object.changedFlags) ? globalThis.Number(object.changedFlags) : 0,
      gameKey: isSet(object.gameKey) ? GameKey.fromJSON(object.gameKey) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      joinAvailableFlag: isSet(object.joinAvailableFlag) ? globalThis.Number(object.joinAvailableFlag) : 0,
      matchmakeType: isSet(object.matchmakeType) ? globalThis.Number(object.matchmakeType) : 0,
      joinGameId: isSet(object.joinGameId) ? globalThis.Number(object.joinGameId) : 0,
      joinGameMode: isSet(object.joinGameMode) ? globalThis.Number(object.joinGameMode) : 0,
      ownerPid: isSet(object.ownerPid) ? globalThis.Number(object.ownerPid) : 0,
      joinGroupId: isSet(object.joinGroupId) ? globalThis.Number(object.joinGroupId) : 0,
      applicationArg: isSet(object.applicationArg)
        ? Buffer.from(bytesFromBase64(object.applicationArg))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: NintendoPresence): unknown {
    const obj: any = {};
    if (message.changedFlags !== 0) {
      obj.changedFlags = Math.round(message.changedFlags);
    }
    if (message.gameKey !== undefined) {
      obj.gameKey = GameKey.toJSON(message.gameKey);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.joinAvailableFlag !== 0) {
      obj.joinAvailableFlag = Math.round(message.joinAvailableFlag);
    }
    if (message.matchmakeType !== 0) {
      obj.matchmakeType = Math.round(message.matchmakeType);
    }
    if (message.joinGameId !== 0) {
      obj.joinGameId = Math.round(message.joinGameId);
    }
    if (message.joinGameMode !== 0) {
      obj.joinGameMode = Math.round(message.joinGameMode);
    }
    if (message.ownerPid !== 0) {
      obj.ownerPid = Math.round(message.ownerPid);
    }
    if (message.joinGroupId !== 0) {
      obj.joinGroupId = Math.round(message.joinGroupId);
    }
    if (message.applicationArg.length !== 0) {
      obj.applicationArg = base64FromBytes(message.applicationArg);
    }
    return obj;
  },

  create(base?: DeepPartial<NintendoPresence>): NintendoPresence {
    return NintendoPresence.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NintendoPresence>): NintendoPresence {
    const message = createBaseNintendoPresence();
    message.changedFlags = object.changedFlags ?? 0;
    message.gameKey = (object.gameKey !== undefined && object.gameKey !== null)
      ? GameKey.fromPartial(object.gameKey)
      : undefined;
    message.message = object.message ?? "";
    message.joinAvailableFlag = object.joinAvailableFlag ?? 0;
    message.matchmakeType = object.matchmakeType ?? 0;
    message.joinGameId = object.joinGameId ?? 0;
    message.joinGameMode = object.joinGameMode ?? 0;
    message.ownerPid = object.ownerPid ?? 0;
    message.joinGroupId = object.joinGroupId ?? 0;
    message.applicationArg = object.applicationArg ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseFriendInfoWiiU(): FriendInfoWiiU {
  return { nnaInfo: undefined, presence: undefined, status: undefined, becameFriend: undefined, lastOnline: undefined };
}

export const FriendInfoWiiU: MessageFns<FriendInfoWiiU> = {
  encode(message: FriendInfoWiiU, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nnaInfo !== undefined) {
      NNAInfo.encode(message.nnaInfo, writer.uint32(10).fork()).join();
    }
    if (message.presence !== undefined) {
      NintendoPresenceV2.encode(message.presence, writer.uint32(18).fork()).join();
    }
    if (message.status !== undefined) {
      Comment.encode(message.status, writer.uint32(26).fork()).join();
    }
    if (message.becameFriend !== undefined) {
      Timestamp.encode(toTimestamp(message.becameFriend), writer.uint32(34).fork()).join();
    }
    if (message.lastOnline !== undefined) {
      Timestamp.encode(toTimestamp(message.lastOnline), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendInfoWiiU {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendInfoWiiU();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nnaInfo = NNAInfo.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.presence = NintendoPresenceV2.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = Comment.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.becameFriend = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.lastOnline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendInfoWiiU {
    return {
      nnaInfo: isSet(object.nnaInfo) ? NNAInfo.fromJSON(object.nnaInfo) : undefined,
      presence: isSet(object.presence) ? NintendoPresenceV2.fromJSON(object.presence) : undefined,
      status: isSet(object.status) ? Comment.fromJSON(object.status) : undefined,
      becameFriend: isSet(object.becameFriend) ? fromJsonTimestamp(object.becameFriend) : undefined,
      lastOnline: isSet(object.lastOnline) ? fromJsonTimestamp(object.lastOnline) : undefined,
    };
  },

  toJSON(message: FriendInfoWiiU): unknown {
    const obj: any = {};
    if (message.nnaInfo !== undefined) {
      obj.nnaInfo = NNAInfo.toJSON(message.nnaInfo);
    }
    if (message.presence !== undefined) {
      obj.presence = NintendoPresenceV2.toJSON(message.presence);
    }
    if (message.status !== undefined) {
      obj.status = Comment.toJSON(message.status);
    }
    if (message.becameFriend !== undefined) {
      obj.becameFriend = message.becameFriend.toISOString();
    }
    if (message.lastOnline !== undefined) {
      obj.lastOnline = message.lastOnline.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<FriendInfoWiiU>): FriendInfoWiiU {
    return FriendInfoWiiU.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FriendInfoWiiU>): FriendInfoWiiU {
    const message = createBaseFriendInfoWiiU();
    message.nnaInfo = (object.nnaInfo !== undefined && object.nnaInfo !== null)
      ? NNAInfo.fromPartial(object.nnaInfo)
      : undefined;
    message.presence = (object.presence !== undefined && object.presence !== null)
      ? NintendoPresenceV2.fromPartial(object.presence)
      : undefined;
    message.status = (object.status !== undefined && object.status !== null)
      ? Comment.fromPartial(object.status)
      : undefined;
    message.becameFriend = object.becameFriend ?? undefined;
    message.lastOnline = object.lastOnline ?? undefined;
    return message;
  },
};

function createBaseFriendInfo3DS(): FriendInfo3DS {
  return {
    pid: 0,
    region: 0,
    country: 0,
    area: 0,
    language: 0,
    platform: 0,
    presence: undefined,
    gameKey: undefined,
    message: "",
    messageUpdatedAt: undefined,
    miiModifiedAt: undefined,
    lastOnline: undefined,
    mii: undefined,
  };
}

export const FriendInfo3DS: MessageFns<FriendInfo3DS> = {
  encode(message: FriendInfo3DS, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pid !== 0) {
      writer.uint32(8).uint32(message.pid);
    }
    if (message.region !== 0) {
      writer.uint32(16).uint32(message.region);
    }
    if (message.country !== 0) {
      writer.uint32(24).uint32(message.country);
    }
    if (message.area !== 0) {
      writer.uint32(32).uint32(message.area);
    }
    if (message.language !== 0) {
      writer.uint32(40).uint32(message.language);
    }
    if (message.platform !== 0) {
      writer.uint32(48).uint32(message.platform);
    }
    if (message.presence !== undefined) {
      NintendoPresence.encode(message.presence, writer.uint32(58).fork()).join();
    }
    if (message.gameKey !== undefined) {
      GameKey.encode(message.gameKey, writer.uint32(66).fork()).join();
    }
    if (message.message !== "") {
      writer.uint32(74).string(message.message);
    }
    if (message.messageUpdatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.messageUpdatedAt), writer.uint32(82).fork()).join();
    }
    if (message.miiModifiedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.miiModifiedAt), writer.uint32(90).fork()).join();
    }
    if (message.lastOnline !== undefined) {
      Timestamp.encode(toTimestamp(message.lastOnline), writer.uint32(98).fork()).join();
    }
    if (message.mii !== undefined) {
      FriendMii.encode(message.mii, writer.uint32(106).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FriendInfo3DS {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFriendInfo3DS();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pid = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.region = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.country = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.area = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.language = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.platform = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.presence = NintendoPresence.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.gameKey = GameKey.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.messageUpdatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.miiModifiedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.lastOnline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.mii = FriendMii.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FriendInfo3DS {
    return {
      pid: isSet(object.pid) ? globalThis.Number(object.pid) : 0,
      region: isSet(object.region) ? globalThis.Number(object.region) : 0,
      country: isSet(object.country) ? globalThis.Number(object.country) : 0,
      area: isSet(object.area) ? globalThis.Number(object.area) : 0,
      language: isSet(object.language) ? globalThis.Number(object.language) : 0,
      platform: isSet(object.platform) ? globalThis.Number(object.platform) : 0,
      presence: isSet(object.presence) ? NintendoPresence.fromJSON(object.presence) : undefined,
      gameKey: isSet(object.gameKey) ? GameKey.fromJSON(object.gameKey) : undefined,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      messageUpdatedAt: isSet(object.messageUpdatedAt) ? fromJsonTimestamp(object.messageUpdatedAt) : undefined,
      miiModifiedAt: isSet(object.miiModifiedAt) ? fromJsonTimestamp(object.miiModifiedAt) : undefined,
      lastOnline: isSet(object.lastOnline) ? fromJsonTimestamp(object.lastOnline) : undefined,
      mii: isSet(object.mii) ? FriendMii.fromJSON(object.mii) : undefined,
    };
  },

  toJSON(message: FriendInfo3DS): unknown {
    const obj: any = {};
    if (message.pid !== 0) {
      obj.pid = Math.round(message.pid);
    }
    if (message.region !== 0) {
      obj.region = Math.round(message.region);
    }
    if (message.country !== 0) {
      obj.country = Math.round(message.country);
    }
    if (message.area !== 0) {
      obj.area = Math.round(message.area);
    }
    if (message.language !== 0) {
      obj.language = Math.round(message.language);
    }
    if (message.platform !== 0) {
      obj.platform = Math.round(message.platform);
    }
    if (message.presence !== undefined) {
      obj.presence = NintendoPresence.toJSON(message.presence);
    }
    if (message.gameKey !== undefined) {
      obj.gameKey = GameKey.toJSON(message.gameKey);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.messageUpdatedAt !== undefined) {
      obj.messageUpdatedAt = message.messageUpdatedAt.toISOString();
    }
    if (message.miiModifiedAt !== undefined) {
      obj.miiModifiedAt = message.miiModifiedAt.toISOString();
    }
    if (message.lastOnline !== undefined) {
      obj.lastOnline = message.lastOnline.toISOString();
    }
    if (message.mii !== undefined) {
      obj.mii = FriendMii.toJSON(message.mii);
    }
    return obj;
  },

  create(base?: DeepPartial<FriendInfo3DS>): FriendInfo3DS {
    return FriendInfo3DS.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FriendInfo3DS>): FriendInfo3DS {
    const message = createBaseFriendInfo3DS();
    message.pid = object.pid ?? 0;
    message.region = object.region ?? 0;
    message.country = object.country ?? 0;
    message.area = object.area ?? 0;
    message.language = object.language ?? 0;
    message.platform = object.platform ?? 0;
    message.presence = (object.presence !== undefined && object.presence !== null)
      ? NintendoPresence.fromPartial(object.presence)
      : undefined;
    message.gameKey = (object.gameKey !== undefined && object.gameKey !== null)
      ? GameKey.fromPartial(object.gameKey)
      : undefined;
    message.message = object.message ?? "";
    message.messageUpdatedAt = object.messageUpdatedAt ?? undefined;
    message.miiModifiedAt = object.miiModifiedAt ?? undefined;
    message.lastOnline = object.lastOnline ?? undefined;
    message.mii = (object.mii !== undefined && object.mii !== null) ? FriendMii.fromPartial(object.mii) : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = BigInt(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds.toString()) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
