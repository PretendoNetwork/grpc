syntax = "proto3";

package miiverse.v2;

message UserContent {
  uint32 pid = 1;
  repeated string followed_communities = 2;
  repeated uint32 followed_users = 3;
  repeated uint32 following_users = 4;
}
